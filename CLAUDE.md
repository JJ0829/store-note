# 프로젝트 컨텍스트

> ## ⚠️ 먼저 읽을 것
>
> **이 파일보다 [docs/00_PROJECT_CURRENT.md](docs/00_PROJECT_CURRENT.md)가 상위 문서다.**
> 사용자가 별도의 단계별 검증 프레임워크(STEP 0~13, Gate, PROVISIONAL/CONFIRMED)를 돌리고 있고,
> 프로젝트의 공식 현재 상태·다음 할 일은 거기에 있다. 이 `CLAUDE.md`는 그 아래의
> **기술 메모 + 결정 근거 모음**이다.
>
> ### 2026-08-31 갱신 — "n=1"은 틀린 기록이었다
>
> **대표자는 호스피탈리티 16년 이상 경력자다.** **베이커리 · 브런치 카페 · 레스토랑**에서
> **셰프·주방장(헤드)**으로 일했다. 즉 신입을 **가르치는 쪽**에 16년 있었다.
> 즉시 실증 가능한 매장도 1~2곳 있다. → [D-006](docs/08_DECISION_LOG.md)
>
> 이 문서와 `docs/`가 근거를 "본인 경험 n=1"로 적어둔 것은 **사실과 달랐다. 전부 정정됐다.**
>
> **그래서 무엇이 달라지는가:**
> - 이 파일 아래의 "확정된 결정"들, 그리고 "카페·베이커리라서 특별한 것"의 리드타임·계량 서술은
>   추측이 아니라 **16년 현업 관찰**이다. 함부로 되묻거나 약화시키지 말 것.
> - **타겟(개인 카페·베이커리)이 실제 근무 업종과 일치한다.** 확인 완료.
> - Gate 1은 여전히 🔬 PROVISIONAL이지만, **"앞으로도 CONFIRMED 불가"는 철회됐다.**
>   실증 매장 데이터가 나오면 판정한다.
>
> **그래도 달라지지 않는 것 (더 중요):**
> - **B4(점주가 왜 돈을 내는가)는 하나도 안 풀렸다.** 주방장은 예산 집행자가 아니다.
>   경력이 길다는 사실을 *점주가 돈을 낸다*는 근거로 쓰지 말 것. 이게 가장 흔한 혼동이다.
> - 여전히 **한 사람의 관찰**이다. 매장에서 나온 측정 데이터가 아니다.
> - README의 편집화면·AI 초안은 여전히 보류다 (D-002 유지).
>
> **✅ 배포한다 — D-007 확정 (2026-09-10, 사장님).**
> 과정 공고문이 지원 항목으로 *"프로토타입을 **실서비스 수준으로 고도화, 클라우드 배포
> 환경 구축**"* 을 명시하고, **9/12 커리큘럼에 "서비스 배포 환경 구축"** 이 박혀 있다.
> Demo Day(9/18)에서 로컬 서버로 시연하면 그 자체가 감점이다.
> 배포 게이트(`01` §10.3)는 전부 닫혔고 **누르는 순서는 [docs/배포.md](docs/배포.md)** 에 있다.
> ⚠️ **레시피 수치가 아직 플레이스홀더라서 지금은 샐 영업비밀이 없다.**
> 실매장 수치를 넣는 순간 사정이 달라진다 — 매장 PIN 은 가림막이고 서버가 HTML 에
> 재료를 실어 보낸다. 그때는 서버 인증이 먼저다.

**매장수첩** — 개인 카페·베이커리의 매장 운영 도구.

| | |
|---|---|
| 제품명 | **매장수첩** (2026-09-06 확정. 이전 가칭 `프렙노트`) |
| 폴더 | `C:\Users\user\Desktop\store-note` |
| 저장소 | `github.com/JJ0829/store-note` — **비공개.** 레시피가 영업비밀이다 |
| 발표 | **2026-09-18 Demo Day** — MVP 시연 + IR 피칭 |
| 과정 | **바이브코딩 창업교육** (2026 SW인재키움사업 · 포항TP + 구미전자정보기술원) |
| 상금 | **우수팀 10팀 내외에 팀당 최대 4천만원.** 50명 모집 → 10팀. **Demo Day 가 곧 선정이 아니다 — 수료 후 별도 신청·평가** |
| 자금 제약 | ★ **인건비·SW·비품 등 자산성 물품 지원 불가.** 클라우드/AI API·컨설팅·디자인·마케팅·IP·IR 만 |
| 자격 | **경북 내 창업.** 예비창업자면 26.12월 내 사업자등록. 타 창업지원사업 중복 불가 |

처음엔 신입 온보딩 도구였는데 운영 기능 7종이 붙으면서 **매장 전체**를 다룬다.
그래서 '주방'이 아니라 '매장'이다 — 홀 직원도 출퇴근을 찍고, 매출·거래처·계약서는 주방 밖 일이다.

해결하려는 문제는 원래 "신입 교육에 시간이 많이 든다"였는데, 논의를 거쳐 이렇게 다시 정의됨:

> **주방의 모든 기준이 사람 머릿속에 있다.** 신입 교육이 힘든 건 증상 하나일 뿐이다.
> 프렙 양은 김대리만 알고 발주 수량은 사장님만 알아서, 그 사람이 쉬면 주방이 흔들린다.
> 이직률이 높다는 게 이 문제를 치명적으로 만든다.

---

## 현재 상태

`npm run dev` 하면 도는 상태. **아직 아무데도 배포 안 함** — D-007 OPEN.
GitHub 비공개 저장소는 백업용이고 배포가 아니다.

| 화면 | 경로 | 상태 |
|---|---|---|
| 사장님 화면 (첫 화면) | `/` | 동작 |
| 교육 모드 (태블릿, 한 장씩) | `/t/[slug]` | 동작 |
| 체크리스트 (개인 링크, 스크롤) | `/p/[slug]` | 동작 |
| 프렙 (리드타임) | `/prep/[slug]` | 동작 |
| **프렙 목록 + 레시피 찾기** | `/prep` | 동작 · 🔑 |
| 레시피 (배수 계산) | `/r`, `/r/[slug]` | 동작 · 🔑 |
| 근무표 (계획) | `/roster` | 동작 |
| 촬영 진행 | `/shoot` | 동작 |
| **출퇴근·근태** (실제) | `/attendance` | 동작 · 인건비만 잠김 |
| **내보내기·되돌리기** | `/backup` | 동작 · 🔒 |
| **근로계약서** | `/contracts` | 동작 · 🔒 |
| **매출** (남은 돈) | `/sales` | 동작 · 🔒 |
| **원가** | `/cost` | 동작 · 🔒 |
| **발주 체크** | `/order` | 동작 |
| **거래처·단가** | `/vendors` | 동작 · 🔒 |
| **촬영 목록 만들기 (AI)** | `/shoot` 안 · `POST /api/shoot-plan` | 동작 · 키 필요 |
| 지표 수집 | `/api/log` | 동작 (로컬 파일) |

**2026-09-04에 운영 기능 7종을 붙였다** (원가·근태·출퇴근·매출·발주·거래처·근로계약서).
7개를 따로 만들지 않고 **이미 있는 데이터에 붙였다**는 게 핵심이다.

```
근무표(계획) ──┐
               ├─ 출퇴근(실제) ─ 차이 = 근태 ─┐
근로계약서(시급)┘                             ├─ 인건비 ─┐
                                                          ├─ 매출 − 재료비 − 인건비
거래처(단가) ─ 레시피 재료 ─ 원가 ─ 재료비 ──────────────┘   = 오늘 남은 돈
             └ 프렙의 kind:"order" ─ 발주 체크
```

**이게 B4(점주가 왜 돈을 내는가)를 처음으로 정면에서 건드린다.** 주방장은 예산
집행자가 아니지만 점주는 원가율과 인건비율을 본다. 다만 아직 매장에서 검증 안 됨.

브라우저에서 실제로 확인한 것: 체크 토글·진행률·localStorage 저장, 교육 모드 필수항목 확인 게이트, 완료 설문, `data/events.jsonl`에 이벤트 기록, 다크모드, 프로덕션 빌드 통과(타입 에러 0).

**시드는 카페·베이커리로 교체 완료다** (`data/seed.json`).
★ **개수의 정본은 [`docs/deliverables/21_화면명세.md` §1-b](docs/deliverables/21_화면명세.md) 한 곳이고 `tests/seed.test.ts` 가 못 박는다.** 아래 숫자는 그 표의 사본이다 — 어긋나면 §1-b 가 맞다.
매장 `○○ 베이커리 카페` · 포지션 3(오픈조·마감조·제빵) · 레시피 **10**(아메리카노·카페라떼·
콜드브루·식빵 + **르방·수제청·냉침차·밀크티 베이스·설탕 시럽·크림폼**) · 프렙 **3목록**(오후 **카드 5 + 옵션 6** / 마감 준비 3 / 주기 **묶음 3 + 항목 13**) · 근무조 4.

⚠️ **매장명과 레시피 수치는 아직 플레이스홀더다.** 실매장 값으로 바꾸는 건 사장님 검수가 필요하다.

---

## 확정된 결정 (그리고 이유)

여러 턴에 걸쳐 뒤집힌 것들이라 이유까지 남겨둠. **되묻지 말 것.**

**네이티브 앱 안 만든다.** 신입이 첫날 앱 설치할 확률보다 링크 누를 확률이 훨씬 높다. 모바일 웹 + 필요하면 나중에 PWA.

**타겟은 개인 매장. 프랜차이즈 아님.** 프랜차이즈는 본사가 사진 매뉴얼을 이미 만들어준다 — 해결된 문제다. 본사에 파는 길(배포 채널)도 있지만 영업 사이클이 길어서 지금은 아님.

**태블릿이 1순위 기기.** 매장에 태블릿은 이미 다 깔려 있다(사용자 확인). 개인폰은 주방에서 젖은 손·위생 규정 때문에 불리. 카톡 공유 링크는 보조 수단.

**경쟁자는 벽에 붙은 코팅 체크리스트다.** 그래서 *체크리스트를 준다*는 것 자체는 새 가치가 0이다. 새로 얹는 건 딱 셋: **사진·영상 / 수정 비용 0 / 기록**.

**영상이 본체, 글은 자막이다.** 주방 교육은 읽기가 아니라 시연·모방이다. 기준이 사람마다 다른 이유는 매뉴얼이 없어서가 아니라 각자 머릿속 시연이 달라서다. 그러니 만들 것은 "더 좋은 문서"가 아니라 **가장 잘하는 사람의 시연을 박제한 것**.

**촬영 모드가 편집 화면보다 먼저다.** 영상이 본체면 병목은 "사장님이 안 찍는다"로 옮겨간다. `<input type="file" accept="video/*" capture>`로 폰 기본 카메라를 열어서, 편집 없이 항목당 30초씩, 주방 한 바퀴 도는 동안 다 찍히게.

**첫 촬영은 신입한테 시킨다.** 선배가 시연하고 신입이 찍는다. 어차피 시연은 하니까 추가 비용 0이고, 신입은 더 집중하고, 다음 신입이 그걸 본다. 콜드스타트가 공짜로 풀린다.

**AI의 역할은 텍스트 초안이 아니라 촬영 목록 생성이다.** 포지션명 넣으면 "이 18개를 찍으세요"가 나오고 폰 들고 한 바퀴. AI와 촬영이 한 흐름.
→ ✅ **2026-09-10 구현.** `/shoot` 아래 · `POST /api/shoot-plan` · `src/lib/shootPlan.ts`.
**SDK를 안 깔았다** — REST라 `fetch`로 된다. 런타임 의존성은 여전히 3개다.
키(`ANTHROPIC_API_KEY`)는 **서버에만** 둔다. `NEXT_PUBLIC_`을 붙이면 번들에 박힌다.
**만든 목록을 저장하지 않는다** — AI가 만든 것을 앱이 기정사실로 만들면 기준이
사람 머릿속에 있다는 문제를 푸는 게 아니라 **AI 머릿속으로 옮기는 것**이 된다.
⚠️ **단일 파일에는 없다.** 그 파일의 규율은 외부 요청 0건이다.

**바에서 만드는 부재료도 레시피를 갖는다** (2026-09-09). 청·냉침차·밀크티·시럽·크림폼·르방.
프렙 항목에서 **그 자리에서 배수를 눌러 보고 만들 수 있어야** 한다 — 부재료야말로 매일
수량이 바뀌는데, 비율을 아는 사람은 한 명뿐이라는 게 이 제품이 푸는 문제 그 자체다.

**하단 탭바는 매일 누르는 것만 둔다** (2026-09-10). `오늘 · 프렙 · 발주 · 출퇴근 · 운영`.
근무표는 주 1회, 교육은 신입 올 때만이라 탭에서 뺐고(홈에서 간다), **하루 두 번 찍는
출퇴근과 매일 아침 여는 발주를 넣었다.** 교육 모드·체크리스트에는 탭바를 안 그린다 —
체크리스트는 카톡으로 **직원 개인 폰**에서 여는 화면이라 거기에 매출 탭을 띄우면 안 된다.

**프렙 안에서 레시피를 끝까지 본다** (2026-09-10). 재료·배수만이 아니라 **만드는 순서까지** 프렙 항목 안에 있다. 나눠 놓으면 주방에서 화면을 옮겨 다녀야 하고, 레시피 화면은 매장 PIN 이 걸려 있어 번호까지 넣어야 했다. 재료와 순서는 한 몸이다. ⚠️ 그래서 **매장 PIN 은 실질적으로 `/r` 화면 하나만 가린다.** 원래부터 가림막이었다 — 서버가 HTML 에 재료를 실어 보낸다(실측 2026-09-07).

**레시피가 있어야 한다.** 체크리스트만 있으면 신입 올 때만 열려서(월 1회) 사장님이 존재를 잊는다. 레시피는 매일 열린다. 메뉴 30개는 벽에 못 붙이므로 여기선 태블릿이 종이를 확실히 이긴다.

**레시피는 공개 링크로 두면 안 된다.** 영업비밀이다. 체크리스트는 링크가 새도 상관없지만 레시피가 새면 사고다. **체크리스트=공개 링크, 레시피=매장 PIN**으로 나눈다. "회원가입 없이"라는 원래 MVP 전제가 레시피 쪽에서는 깨진다.

**프렙 리스트가 가장 강한 데일리 훅이다.** 매일 아침 반드시 열리고, 숙련자도 쓰고, **수량이 매일 바뀌어서 종이로 못 한다.** 다른 기능은 다 종이로 어느 정도 된다.

**오더는 프렙의 상류다.** 프렙 수량이 정해지면 발주가 따라 나온다. 따로 만들 필요 없고 나중에 자동으로 붙는다.

**"시간표"는 직원 근무 스케줄이다** (업무 시간표 아님). 처음에 제외했던 "급여/스케줄 시스템 **연동**"과는 다른 것이므로 모순 아님. 그리고 부가 기능이 아니라 **어떤 화면을 먼저 띄울지 고르는 기준**이 된다.

**공용 태블릿이라 저장 방식이 화면마다 다르다.** 교육 모드가 localStorage에 진도를 남기면 앞사람 체크가 다음 신입에게 보인다. 그래서 교육 모드는 sessionStorage + `runId`(시작할 때마다 새로 발급, 끝나면 삭제). 체크리스트는 localStorage(날짜별 초기화). 검증 완료.

---

## 카페·베이커리라서 특별한 것

**리드타임이 이 업종의 핵심.** 오늘 하는 프렙의 절반은 내일을 위한 것이다.

| 항목 | 리드타임 | 안 하면 |
|---|---|---|
| 콜드브루 추출 시작 | 12~24h | 내일 콜드브루 판매 불가 |
| 반죽 제조 후 냉장 | 12~18h | 내일 아침 빵 없음 |
| 르방 갱신 | 매일 | 복구에 2~3일 |
| 냉동 생지 해동 | 12h | 내일 해당 품목 없음 |

**오늘은 아무 일도 안 일어나고 내일 아침에 터진다.** 신입이 스스로 알 방법이 없고, 어제 뭘 걸어놨는지 기억에 의존해서 종이로는 못 잡는다. **이게 제품이 종이를 이기는 가장 확실한 지점.**

**계량·배수가 필수.** 베이킹은 g 단위, "1배합=식빵 6개"인데 9개 필요하면 1.5배. 다른 업종이면 나중 기능인데 여기선 없으면 레시피가 반쪽.

**영상으로 박제할 1순위 4개** (기준이 가장 안 맞는 항목):
추출 테스트 합격 기준 / 스팀 밀크 온도·거품 / 베이커리 잔여 할인·폐기 전환 / 반죽 발효 완료 판단.

자세한 하루 흐름은 [docs/day-flow.md](docs/day-flow.md).

---

## 사용자에게 받은 답 (시드에 반영됨)

`docs/day-flow.md` 확인 요청 4건 — **전부 답변 받았고 시드에 들어갔다.**

| 물어본 것 | 답 |
|---|---|
| 교대 시간·인원 | 제빵을 따로 둔다. 미들조 있음 → 시드의 근무조 4개 |
| 추출 테스트 | 매일이 아니라 **재고에 따라** → `trigger: condition` |
| 발주 요일 | 평일은 주문 시각이 정해져 있고 **금요일에 주말치까지 몰아서** |
| 리드타임 항목 | 4개가 다가 아니었다 → 주기 점검 13개로 늘림 |

★ **쿠팡이 이 제품의 논리를 바꿨다.** 웬만한 건 쿠팡에서 다 판다.
그래서 "발주 사고 방지"는 약한 가치다 — 깜빡해도 돈으로 메운다.
**진짜 가치는 `recoverable: false`**, 즉 **돈으로 되돌릴 수 없는 것**이다
(콜드브루·반죽·르방). 화면이 그것만 빨간 테두리로 띄운다.

---

## 다음 할 일 — 2026-09-06 기준

> 원래의 9단계는 **1~8이 끝났다.** 9번(Supabase)은 배포 게이트(D-007) 뒤에 있다.
> 아래는 발표(9/18)를 기준으로 다시 세운 것이다.

### 지금 남은 것 — 내가 할 수 있는 것

| # | 무엇 | 게이트 | 비고 |
|---|---|---|---|
| ~~1~~ | ~~V-12 — 단일 파일의 Google Fonts~~ | ✅ **완료 2026-09-07** | **(가) 링크 제거 + 시스템 폰트 스택.** 두 단일 파일 모두 **외부 요청 0건**이 됐다. 우려됐던 계량 표 열 정렬은 실측상 안 깨진다(`tabular-nums`가 폴백 서체에서도 산다) → `06` V-12 |
| 2 | 발표 자료 자체 (13분 구성) | 발표 전 | 아직 착수 안 함 |
| 3 | PIN 게이트를 **진짜** 접근 통제로 | 배포 후 | 지금 것은 가리개다 (`06` V-19). 서버가 있어야 한다. **★ 레시피 쪽은 더 약하다** — 서버가 HTML에 실어 보내므로 잠긴 상태에서도 페이지 소스에 재료가 들어 있다 (실측 2026-09-07) |
| ~~4~~ | ~~출퇴근·계약 내보내기(백업)~~ | ✅ **완료 2026-09-07** | `/backup` 신설. **CSV(엑셀 보관·제출용) + JSON(되돌리기용).** 되돌리기는 JSON만 받는다 — 엑셀에서 한 칸 고치면 시각·요일이 깨지고 근태·인건비가 조용히 틀린다. 덮어쓰기이므로 확인 두 번 + **잃는 건수를 그 순간 다시 읽어서** 보여준다 |
| 5 | Supabase | D-007 확정 후 | `src/lib/repo.ts` 와 각 lib 의 load/save 만 고치면 된다 |

★ **인수인계는 [docs/HANDOFF.md](docs/HANDOFF.md) 다.** 새 채팅창은 그 파일 하나로 시작할 수 있다.

**배포 전 조치의 정본은 [`docs/deliverables/01_MVP기획서.md` §10.3](docs/deliverables/01_MVP기획서.md)이다.**
목록이 다섯 문서로 흩어졌던 것을 거기 (가)(나)(다) 세 표로 합쳤다. **배포 체크는 그 표 하나만 본다.**

### 사장님만 할 수 있는 것 (내가 못 함)

| 무엇 | 왜 급한가 |
|---|---|
| **촬영 4개** | "사진·영상"이 핵심 가치 셋 중 하나인데 **실사가 0장**이다. 발표 화면이 회색 네모로 찬다. 앱의 `촬영 진행` 화면에 파일명이 다 나온다 |
| **레시피 수치 검수** | 심사위원이 물어볼 숫자다. 16년 경력으로 봐주셔야 한다 |
| **시연 기기 데이터 정리** | 실명·시급·매출·거래처 단가가 화면에 뜬다. **잠금은 시연 중 해제 상태라 보호가 안 된다** |
| **실증 매장 투입** | ★ **남은 가장 큰 구멍.** 아래 참조 |
| **main 머지 + Vercel 배포** | 내가 못 한다(토큰·브라우저 인증). 누르는 순서는 [docs/배포.md](docs/배포.md) |
| **수익모델** | ★ 4천만원 심사의 핵심. **인건비·자산성 물품 지원 불가** 제약 안에서 짜야 한다 |
| **결정** | ~~V-12 폰트~~ · ~~`/roster` 잠금~~ (2026-09-07 완료) · ~~**D-007 배포 여부**~~ (2026-09-10 **배포한다**로 확정) / 남은 것: **이용기록 보유기간 숫자** · **PIN 분실 처리 정책** · **법정 주기 확인** |

### ★ 반복해서 경계할 것 — 이게 여전히 가장 중요하다

**기능은 7개 늘었는데 매장 투입은 아직 0곳이다.**

문제 자체는 16년 경력으로 안다. 하지만 *이 제품이 그 문제를 푸는지*는 다른 질문이고,
**B4(점주가 왜 돈을 내는가)는 하나도 안 풀렸다.**

운영 기능이 점주가 결정할 때 보는 숫자(원가율·인건비율·남은 돈)를 화면에 올린 건 맞다.
**그런데 그건 화면을 만든 것이지 지불 의사의 근거가 아니다.**
오히려 화면이 그럴듯한 숫자를 보여주니 "점주가 좋아할 것"이라는 확신이 근거 없이 커진다.
→ `docs/deliverables/01_MVP기획서.md` §3.4-B

**기능 추가 요청이 오면 "매일 열리는 이유"를 늘리는지 먼저 따질 것.**
지금 매일 열리는 것: 프렙 · 출퇴근(하루 2번) · 발주(아침) · 매출(마감).
드물게 열리는 것: 원가 · 거래처 · 계약서.

---

## 실행 / 환경 주의사항

```bash
npm run dev
```

- `http://localhost:3000` 사장님 화면 / `/t/cafe-open` 교육 모드 / `/p/cafe-open` 체크리스트
  (슬러그는 `data/seed.json`의 `shareSlug` — `cafe-open` · `cafe-close` · `bakery-morning`)
- **Node는 winget으로 새로 설치했다.** 셸에서 `node`가 안 잡히면 PATH를 새로고침해야 함:
  `$env:Path = [Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [Environment]::GetEnvironmentVariable("Path","User")`
- 그래서 `.claude/launch.json`은 `node.exe` **절대경로**를 쓴다. 건드리지 말 것.
- **dev 서버를 켜둔 채 `npm run build` 하면 `.next`가 덮어써져서 dev 서버가 죽는다.** 빌드 전에 서버를 끌 것.
- **워크플로/서브에이전트가 도는 중에는 빌드하지 말 것.** (2026-09-03에 실제로 당함)
  에이전트가 스스로 `.next`를 지우거나 dev 서버를 띄우기 때문에, 동시에 빌드하면
  매번 다른 오류가 난다 — `_app.js.nft.json` 없음 / `clientReferenceManifest` 불변식 위반 /
  `/_not-found` 모듈 못 찾음. 코드 문제로 착각하기 쉽다.
  **복구법:** 서버·에이전트 다 멈추고 `.next` 완전 삭제 후 다시 시작.
- **타입만 볼 때는 `npx tsc --noEmit`을 쓸 것.** `.next`를 안 건드리므로 dev 서버가 안 죽는다.
- **전체 점검은 `npm run check`** (타입 + 빌드 + 테스트). `tests/builtPages.test.ts` 는
  **빌드 산출물(`.next/server/app/*.html`)을 검사**하므로 빌드가 먼저 돌아야 한다.
  `.next` 가 없으면 그 8개가 **건너뜀으로 표시된다** — `npm test` 만 돌리고
  "전부 통과" 라고 읽으면 화면 검사를 안 한 것이다.
- 큰 파일은 Bash heredoc이 깨진다(따옴표 파싱 에러). Write 도구를 쓸 것.
- **Bash로 `node -e` / `python -c` 안에 백틱을 쓰면 셸이 먹는다.** (여러 번 당함)
  마크다운 코드표기나 JS 템플릿 리터럴이 필요하면 스크립트 파일로 만들어 실행할 것.

## 구조

```
data/seed.json              ← 지금은 이 파일이 DB (카페·베이커리. 매장명은 플레이스홀더)
data/events.jsonl           ← 지표. git 제외
docs/day-flow.md            ← 카페·베이커리 하루 흐름. 시드의 원본
src/lib/repo.ts             ← 데이터 접근. Supabase 갈 때 여기만 고침
src/app/page.tsx            ← 사장님 화면
src/app/t/[slug]/page.tsx   ← 교육 모드
src/app/p/[slug]/page.tsx   ← 체크리스트
src/app/api/log/route.ts    ← 지표 수집
src/components/TrainingMode.tsx
src/components/ChecklistView.tsx
public/photos/              ← 회색 플레이스홀더. 실제 사진으로 교체

── 운영 기능 (2026-09-04 추가) ──────────────────────────────
src/lib/store.ts            ← localStorage 공통 + 원/% 표기 + 한글 조사(ro/eul/eun/gwa)
src/lib/units.ts            ← 단위 환산. ★ 계열이 다르면 계산 안 함 (g↔ml 금지)
src/lib/vendors.ts          ← 거래처 + 품목 단가 + 발주 도착일
src/lib/cost.ts             ← 레시피 원가. ★ 못 구한 값을 0원으로 안 셈
src/lib/attendance.ts       ← 출퇴근·근태·인건비(추정)
src/lib/contracts.ts        ← 근로계약 조건 + 법정 점검. ★ 주민번호 칸 없음
src/lib/sales.ts            ← 매출 − 재료비 − 인건비
src/lib/orders.ts           ← 발주 체크 (주문함 / 들어옴 두 단계)
src/lib/settings.ts         ← 최저임금·5인이상·목표원가율·판매가·원가제외 재료
src/lib/businessDay.ts      ← ★ 매장의 하루. 경계는 자정이 아니라 새벽 4시
src/lib/cycleDone.ts        ← ★ 주기 점검의 **마지막으로 한 날 + 매장이 정한 주기**
src/lib/shootPlan.ts        ← ★ AI 응답 거르기. **프롬프트보다 여기가 위험하다**
src/app/api/shoot-plan/     ← ★ 유일한 AI 호출. 키는 서버에만
src/lib/shiftClock.ts       ← ★ 자정을 넘는 근무조 판정 (연장된 날의 마감조)
src/lib/backup.ts           ← 내보내기·되돌리기. ★ CSV는 BOM 필수 + 수식 주입 차단
src/lib/pinDigest.ts        ← PIN 단방향 요약값. 두 잠금이 공유 (소금은 다르게)
src/lib/ownerGate.ts        ← 사장님 PIN. ★ 보안이 아니라 가리개다
src/lib/storeGate.ts        ← ★ 매장 PIN (레시피). 사장님 PIN과 다른 것이다
src/components/ui.tsx       ← Screen/Card/Row/Chip/NumField 공통
src/components/OwnerGate.tsx← 화면 전체 잠금 + InlineUnlock(부분 가리기)
src/components/StoreGate.tsx← 레시피 가림막 + StoreLockButton
src/components/BackupView.tsx← 내보내기·되돌리기
tests/                      ← 425개. lib 전부 + 시드 숫자 정본 + 단일 파일 구문 검사 (2026-09-10)
```

### 운영 기능에서 조심할 것 (테스트로 못 박아둠)

- **g과 ml은 절대 안 바꾼다.** 밀도를 모른다(밀가루 0.55, 우유 1.03). 바꾸면
  원가가 조용히 틀리고 사람이 못 알아챈다. 화면에 "단위가 안 맞습니다"를 띄운다.
- **단가를 못 구한 재료를 0원으로 세지 않는다.** 그러면 원가율이 실제보다 낮게
  나오고, 사장님이 그 숫자로 가격을 정한다. `missing`을 항상 같이 돌려준다.
- **★ "추출량"에 단가를 붙이면 원두가 두 번 계산된다.** 아메리카노 시드에
  "원두(도징) 18g"과 "추출량 36g"이 둘 다 있는데 추출량은 사는 게 아니라 결과다.
  `settings.excluded`로 빼놓는다. 거래처 화면의 `안 셈` 버튼이 그것.
- **자정을 넘기는 마감조.** 23:00→01:00을 그냥 빼면 −22시간이고 인건비가 깎인다.
- **5인 미만은 연장 가산수당이 없다** (근로기준법 제11조). 개인 카페 기본값.
- **인건비는 추정이다.** 4대보험·소득세·수습감액·연차수당 없음. 급여 대장 아님.
- **주민등록번호 칸을 만들지 않았다.** 개인정보보호법 제24조의2가 암호화를
  요구하는데 localStorage로는 못 맞춘다. 계약서 원본은 앱 밖에 둔다.
- **★ CSV 맨 앞의 BOM을 지우지 말 것.** 없으면 엑셀이 UTF-8을 못 알아보고
  한글이 전부 깨진다. 사장님이 "안 된다"고 판단하는 가장 흔한 지점이다.
  (`Blob.text()`는 BOM을 떼고 디코딩하므로 테스트는 바이트로 봐야 한다)
- **★ CSV 칸이 `= + - @`로 시작하면 앞에 `'`를 붙인다.** 엑셀·구글시트가
  수식으로 실행한다. 직원이 메모를 입력하는 칸이 있어서 실제로 걸리는 경로다.
- **★ 매장의 하루는 새벽 4시에 바뀐다.** 자정이 아니다 — 마감조가 특별한 경우 01:00 까지 일한다.
  체크 상태 키는 `businessDay()`로 만든다. 달력 날짜(`new Date()`)를 그대로 쓰면
  23:50에 찍은 체크가 00:10에 사라져 보인다. 출퇴근은 원래부터 자정을 처리했는데
  체크리스트·프렙만 안 하고 있었다. (`19_정책정의서.md` §3.2)
- **★ 마감조가 자정을 넘을 수 있다 (평소 14:30~22:30, 예외 01:00 퇴근).** 근무조 판정을
  `cur >= start && cur < end` 로 하면 **마감조가 화면에서 영영 안 뜬다** —
  start 1020, end 60 이라 어느 시각에도 참이 안 된다. `shiftClock.isOnDuty()` 를 쓸 것.
  영업은 22:00 종료 · 마감 22:30 완료 · 01:00 퇴근은 특별한 경우 (사장님 확인 2026-09-09).
  **하루 단위 화면(체크리스트·프렙·발주)은 전부 `businessDay()` 를 쓴다.** 달력 날짜 금지.
- **★ 옵션은 흩지 말고 한곳에 모은다.** 바에서 만드는 부재료 다섯(에이드 청·냉침차·
  밀크티 베이스·시럽·크림폼)은 **`바 부재료 점검 · 제조`(`p-bar`) 한 카드**에 모여 있다.
  각각 카드를 차지하면 오후 프렙이 열 칸이 되고, 그중 대부분은 "남아 있으니 넘어감"
  이라 **진행률이 의미를 잃는다.** 사장님이 실제로 하는 일도 하나다 —
  *"점심손님 몰리는 이후 모든거 체크하고 만들지 않냐"*.
- **★ `optionOf` 와 `optional` 은 다른 것이다.** 하나는 배치, 하나는 셈이다.
  `optionOf` = **어느 카드 안에 들어가는가** / `optional` = **진행률 분모에 들어가는가**.
  바 부재료·르방은 `optional: true`(안 파는 메뉴가 있으니 분모에서 빠진다).
  **주기 점검 13개는 묶여 있지만 `optional: false`** — 보건증·소방은 매장 사정과
  무관하게 해야 한다. 그래서 화면 문구도 다르다: 옵션은 "매장에 따라 안 하기도
  합니다", 묶음은 "이 묶음의 항목 n개".
  **자식이 `optional:false` 인 카드는 「묶음 머리」다** — 할 일이 아니라 이름표이므로
  누를 수 없고 분모에도 안 들어간다. 부모까지 세면 같은 일을 두 번 센다.
  → 주기 점검 분모는 **16이 아니라 13**이다.
- **★ 매장마다 하거나 안 하는 일은 `optionOf` 로 부모 항목에 붙인다.**
  르방이 그렇다 — 사워도우를 안 하는 카페엔 뜻이 없는데 목록의 한 칸을 차지하면
  **`9/10` 이 영영 안 채워지고 진행률이 거짓이 된다.** 그러면 사람은 진행률을 안 본다.
  옵션은 **부모 카드 안**에 그려지고 **진행률에서 빠진다.** 다만 `recoverable:false`
  옵션은 **빨간 안내에는 그대로 센다** — 르방을 쓰는 매장에서 "다 했습니다"가
  거짓이 되면 안 된다. **두 숫자의 분모가 다른 것은 일부러다.**
  (사장님 지적 2026-09-09: *"르방이 쓰는 곳도 있고 안 쓰는 곳도 있는데 꼭 저렇게 자리 차지 해야 하나"*)
- **★ 프렙의 `kind` 는 리드타임의 종류다. 없으면 `routine` 을 쓴다.**
  `time`·`order`·`cycle` 은 전부 "기다려야 하는 것"이다. 홀 정리·화장실 청소처럼
  **리드타임이 없는 일**에 저 중 아무거나 붙이면 화면이 "몇 시부터 사용 가능"을
  계산해서 **없는 약속**을 만든다. 2026-09-09 에 `routine` 을 더했고,
  `tests/seed.test.ts` 가 리드타임 칸이 둘 다 비어 있는지 확인한다.
- **★ 주기 숫자(`everyDays`)를 시드에 박아두지 않는다. 매장이 정한다.**
  `1개월마다`·`6개월마다` 는 **쓰는 곳마다 다르다** — 제빙기를 매일 닦는 매장에
  `1개월마다` 를 띄우면 그 화면은 처음부터 틀린 말을 한다. 법정 항목도 업종·규모에
  따라 다르다. **모르는 숫자를 앱이 단정하면 사장님이 그걸 믿는다.**
  시드는 전부 `null` 이고 매장이 `sop:cycleEvery` 에 넣는다(화면의 입력칸).
  안 넣으면 기한을 판단하지 않고 **`마지막 5/11 · 121일 전`** 만 보여준다 —
  그것만으로도 아무도 기억 못 하던 것을 대신 기억해 준다.
  (사장님 지적 2026-09-08 · `src/lib/cycleDone.ts`)
- **★ 주기 점검은 "그날 체크" 가 아니라 "마지막으로 한 날" 을 남긴다.**
  `prep:{slug}:{영업일}` 에 넣으면 **다음 날 지워져서 주기를 아예 못 센다.**
  `4개월마다` 라고 적어놓고 앱이 그 4개월을 세지 않으면 그 화면은 읽을 거리다.
  그래서 `sop:cycleDone` = `{ "c-1": "2026-05-11" }` 로 두고 **절대 안 지운다.**
  기록이 없으면 `null` 로 두고 화면이 **"기록 없음 — 언제 했는지 모릅니다"** 라고
  말한다. 0 이나 큰 수로 채우면 "오늘 해야 함" 또는 "여유 있음" 으로 보이는데
  둘 다 거짓이다. (2026-09-08 · `src/lib/cycleDone.ts`)
- **★ "안 하면 —" 은 리드타임이 없는 항목에만 띄운다.**
  `지금 걸면 → 내일 07:43 부터` 가 이미 "오늘 안 하면 내일 못 쓴다" 를 말한다.
  거기에 한 줄 더 붙이면 같은 말을 두 번 하는 것이고, `쿠팡으로 메울 수 있습니다`
  처럼 **안 해도 괜찮다고 알려주는 문장은 해롭다.** (사장님 지적 2026-09-08)
  주기 점검·홀 정리처럼 리드타임이 없는 일에만 남긴다 — 거기서는 그 문장 말고
  왜 하는지 설명할 방법이 없다.
- **되돌리기는 합치지 않고 덮어쓴다.** 같은 날짜 출퇴근이 양쪽에 다르면
  어느 쪽이 맞는지 앱이 모른다. 조용히 고르면 급여가 틀린다.

## 쌓이는 지표

**15종이다.** 표를 손으로 맞추면 또 어긋나므로 `tests/metrics.test.ts` 의 `EXPECTED` 가
코드와 이 표를 같이 못 박는다 — **이벤트를 늘리면 그 테스트가 걸린다.**
보내는 곳은 `src/lib/metrics.ts` 하나다 (컴포넌트가 직접 `/api/log` 를 부르면 테스트가 잡는다).

| event | 뜻 |
|---|---|
| `view` | 체크리스트 링크를 열었다 |
| `check` | 체크 한 건 (`critical`·`doneCount` 동봉 — 어디서 멈췄나) |
| `training_start` | 태블릿 교육 시작 |
| `critical_confirm` | 필수 항목 확인 눌렀다 |
| `training_complete` | 교육 완료 (`durationSec` = 실제 소요) |
| `survey` | "선배에게 몇 번 물어봤나" |
| `prep_view` · `prep_check` · `prep_scale` | 프렙을 정말 매일 여는가 |
| `recipe_view` · `recipe_scale` | 레시피를 정말 매일 여는가 |
| `punch` | 출퇴근 버튼 (`in`/`out` 만. 이름·시각 안 담음) |
| `sales_close` | 매출 칸이 0에서 채워졌다 (칸 이름만. **금액 안 담음**) |
| `order_mark` | 주문함 / 들어옴 전이 |
| `backup` | 전체 백업을 받았다 |

**지표에 이름·금액을 담지 않는다.** "채워졌는가"만 남기고 "무엇이 채워졌는가"는 안 남긴다 —
담기 시작하면 지표 파일이 개인정보·영업비밀 파일이 된다. 이것도 테스트로 못 박아뒀다.

검증 대상 가설: *"신입이 체크리스트+영상만 봐도 선배가 붙어있는 시간이 줄어든다."*
`training_complete.durationSec` + `survey`를 붙여서 본다.

★ **그 둘을 잇는 열쇠가 `runId` 인데 예전엔 빠져 있었다.** `log()` 가 컴포넌트 4개에
복사돼 있었고 교육 모드만 `sessionId` 를 안 붙였다. 아무 오류도 안 나고 이벤트도 잘 쌓여서,
**나중에 분석하려 할 때야 못 잇는다는 걸 알게 되는** 종류의 결함이었다. 그때는 다시 못 모은다.
체크리스트 설문도 완주자에게만 떠서 중도 이탈자 표본이 0건이었다 — 지금은 하나라도 체크하면 뜬다.

참고로 선배가 붙어있는 시간은 **가르치는 시간 + 지켜보는 시간**인데, 보통 뒤엣것이 더 길다.
그래서 "신입이 따라 한 걸 찍어 올리고 선배가 나중에 확인"하는 비동기 구조가 가설을 가장 직접적으로 공격한다. 아직 미구현.
