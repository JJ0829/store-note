# 00. PROJECT CURRENT — 현재 전체 상태

> 본문 최종 갱신: **2026-08-31** · 아래 머리말 갱신: **2026-09-12**
>
> ### ★ 새 작업을 시작한다면 [`docs/HANDOFF.md`](./HANDOFF.md) 부터 본다
>
> 이 문서는 *검증 프레임워크상 어디에 있는가*를 답하고,
> HANDOFF 는 *지금 무엇을 할 차례인가*를 답한다. **본문은 8/31 기준이라 아래가 빠져 있다.**
>
> | 날짜 | 무엇 |
> |---|---|
> | 09-04 | **운영 기능 7종** (원가·근태·출퇴근·매출·발주·거래처·근로계약서) |
> | 09-06 | 제품명 **`매장수첩`** 확정 · 폴더 `store-note` |
> | 09-07 | `/backup` · 매장 PIN · `/roster` 잠금 · **GitHub 비공개 저장소 연결** |
> | 09-08 | **주기 점검을 「마지막으로 한 날」 기준으로 재설계** · 주기 숫자를 시드에서 뺌 |
> | 09-09 | 저장 실패 알림 · 바 부재료 5종 + 레시피 · **영업일 새벽 4시 경계** |
> | 09-10 | **AI 촬영 목록 만들기** · 탭바 정리 · 프렙 안 레시피 · **D-007 「배포한다」로 확정** · 문서 숫자 정본화 · **매장 PIN 을 서버 인증으로**(`ServerStoreGate`) |
> | 09-11 | **스키마 v2** · 검사기 6종(셈·동작·1:1·전수·그림·문서) · 이관 대응표 · 제출용 HTML 생성기 |
> | 09-12 | **지표를 서버 DB 로**(D-002 예외) · **WBS 문서** · `/shoot` 에서 묶음 머리 제외(촬영 **88 → 85**) · 버거집 사진 잔재 삭제 · **시연용 단일 파일에 내보내기 · 촬영 화면**(HANDOFF §3-① 다섯 줄 정리) · 발주 마감 순서 · 화면 폭 튐 · **촬영 목록을 시드에서 생성**(28개 빠져 있었다) |
>
> **지금: 화면 19개 · 테스트 585개 통과 · 타입 오류 0 · 아직 배포 전(작업은 전부 `main` 에 있다).**
> (2026-09-12 `npm run check` 완주 기준 — 빌드 산출물을 보는 8건 포함)
>
> ⚠️ **개수는 낡는다.** 이 줄의 숫자가 의심스러우면 `npm run check` 가 말하는 값이 맞다.
> 다른 문서는 개수를 현재형으로 적지 않기로 했다 (`tests/docs.test.ts` 가 막는다).
> 숫자의 정본은 [`deliverables/21_화면명세.md` §1-b](deliverables/21_화면명세.md) 한 곳이다.

---

## 한 줄 요약

개인 카페·베이커리의 **매장 운영 도구.** 시작은 신입 온보딩 체크리스트였고,
지금은 프렙·레시피·발주·출퇴근·원가·매출까지 다룬다 (2026-09-04 운영 기능 7종).
작동하는 MVP 코드가 있고, **대표자는 호스피탈리티 16년 경력자**다.
문제 근거는 상당히 있으나 **수익모델(점주가 왜 돈을 내는가)은 여전히 근거 0**이다.

## 현재 단계

| | |
|---|---|
| 프레임워크상 위치 | **STEP 1 종료 → STEP 6~7(서비스 구조·데이터 모델)로 이동** |
| 코드 상태 | STEP 13 수준의 Vertical Slice 완성 (선행 구현) |
| 통과한 Gate | Gate 1 🔬 **PROVISIONAL** — 근거는 **16년 현업 관찰**. 독립 검증 대기. [D-006](./08_DECISION_LOG.md) |
| 코드 동결 | **해제됨** (2026-08-30). [D-005](./08_DECISION_LOG.md) |
| 프로젝트 성격 | 실제 창업·사업 검증. **정부지원사업 심사 발표 준비 중** (2026-08-31) |
| 타겟 | 개인 카페·베이커리 — ✅ **대표자 근무 업종과 일치 확인** ([D-006]) |

### 대표자 (2026-08-31 확인 — 이 프로젝트 근거의 출처)

| 항목 | 내용 |
|---|---|
| 업계 경력 | 호스피탈리티 **16년 이상** |
| 업장 유형 | **베이커리 · 브런치 카페 · 레스토랑** |
| 최고 포지션 | **셰프 · 주방장(헤드)** — 즉 신입을 **가르치는 쪽** |
| **오픈 멤버 경험** | **5곳** — 백지에서 주방 기준을 세워본 것이 다섯 번 |
| **이직 경로** | **인맥 요청("와달라")으로 이동** — 지원해서 간 것이 아니다 |
| 즉시 실증 가능한 매장 | **1~2곳** |

> ⚠️ 코드가 앞서 있다고 해서 단계가 진행된 것이 아니다. [D-001](./08_DECISION_LOG.md) 참조.
>
> ⚠️ **2026-08-31 정정.** 이 문서는 근거를 "본인 경험 n=1"로 적고
> "Gate 1은 앞으로도 CONFIRMED가 되지 않는다"고 선언했으나 **그 전제가 사실과 달랐다.**
> 정정 내역은 [D-006](./08_DECISION_LOG.md)에 있다.
>
> ⚠️ **그러나 등급은 올리지 않았다.** 16년이어도 **여전히 한 사람의 관찰**이다.
> CONFIRMED는 실증 매장의 before/after 데이터가 나온 뒤에 판정한다.
>
> ⚠️ **가장 중요 — B4는 하나도 안 풀렸다.** 주방장은 예산 집행자가 아니다.
> 경력이 길다는 사실이 *점주가 돈을 낸다*는 근거가 되지 않는다. 혼동하지 말 것.

## 구현되어 있는 것 ✅

- 신입용 체크리스트 화면 — 섹션 / `critical` 강조 / 선배 한마디 / 사진 / 유튜브 임베드
- 체크 상태 localStorage 저장, 날짜 바뀌면 자동 초기화, **가입 없음**
- 점주용 화면 — 포지션 목록 + 링크 복사
- 카카오톡 미리보기 og 태그
- 데이터 접근 계층 분리 (`src/lib/repo.ts` — Supabase 이관 대비)
- 시드 데이터 3개 포지션 (그릴 / 튀김 / 마감 청소) — **가상 매장 데이터**

### 2026-08-30 세션에서 추가됨 (당시 "코드 건드리지 않는다" 지시 위반 → [D-005]로 사후 해제됨)

- **교육 모드 `/t/[slug]`** — 매장 공용 태블릿용. 한 항목씩 전체 화면, `critical` 항목은
  [읽었습니다] 확인을 눌러야 다음으로 넘어감. 끝나면 설문 후 초기 화면 복귀.
- 공용 기기 대응 — 교육 모드는 sessionStorage + `runId`(시작할 때마다 새로 발급, 끝나면 삭제).
  체크리스트(`/p/`)의 localStorage와 분리되어 앞사람 진도가 다음 사람에게 안 보인다. 검증 완료.
- 지표 이벤트 확장 — `training_start` / `critical_confirm` / `training_complete`(`durationSec` 포함) / `survey`
- `docs/day-flow.md` — 카페·베이커리 하루 흐름 초안 (사용자 확인 대기)
- `CLAUDE.md` — 이 세션에서 나온 결정과 근거. **전부 인터뷰 없이 나온 것이라 PROVISIONAL.**

## 진행 중 🔬

- `docs/00_아이디어_원문.md` — 필수 4항목 중 **3개 완료** (2026-08-31).
  남은 것은 **"가장 답답했던 구체적 장면 한 가지"** — 발표 오프닝과 시드 데이터의 원본이 된다.
- `docs/day-flow.md` — 확인 필요 4개 대기 (교대·추출테스트·발주·리드타임).
  **16년 경력자에게는 즉답 가능한 항목들이다.** 이게 막혀서 데이터 모델이 대기 중이다.
- **경험의 숫자화** — 가르친 신입 수 / 1명당 선배가 붙는 시간 / 조기 이탈 비율 /
  리드타임 누락 사고 횟수. 문제 정의와 발표 ROI 슬라이드가 전부 여기에 걸려 있다.

## 미착수 ❓

STEP 2(사용자 정의) · 3(시장·경쟁) · 4(가치제안) · 5(사업모델) · 6~12 전부.
특히 **STEP 5(누가 왜 돈을 내는가)는 근거가 전무하다.**

## Blocker ⛔

| # | 내용 | 영향 |
|---|---|---|
| B1 | 가설에 baseline이 없어 효과를 측정할 수 없음 | 검증 불가. **실증 매장 1~2곳 확보로 경로는 생겼다** |
| B2 | `/api/log`가 파일 append라 배포 환경에서 무동작 | 지표 0건. *단, 심사 시연용 배포에는 무관* ([D-007]) |
| B3 | ~~타인 근거 없음 (n=1)~~ → **완화됨** | 16년 현업 관찰로 재평가 ([D-006]). 다만 **독립 검증은 여전히 0** |
| B4 | Buyer가 왜 돈을 내는지 미정의 | Gate 4 진입 불가. ⛔ **변동 없음 — 최우선 미해결 과제** |
| B5 | 배포 금지와 심사 발표 시연이 충돌 | 라이브 시연 불가. 사용자 결정 대기 ([D-007]) |

> **B4를 다시 강조한다.** 대표자 경력이 16년이라는 사실이 B4를 조금도 풀지 않는다.
> 셰프·주방장은 **예산 집행자가 아니었다.** 결제 의사는 점주에게서만 나온다.
> 실증 매장 1~2곳이 생겼으므로, 그 점주에게 **금액을 직접 묻는 것**이 가장 빠른 경로다.
>
> **간접 근거는 하나 생겼다.** 대표자가 인맥 요청으로 이동했고 오픈 멤버로 5곳에 참여했다는 것은,
> 점주들이 *주방 기준을 세워줄 사람*에게 이미 돈을 낸 이력이라는 뜻이다.
> **발표에서는 강한 서사지만, H6의 근거로 등재하지 않았다** — 인건비 지불 의사와
> 소프트웨어 구독 지불 의사는 다르기 때문이다. 자세한 이유는 [D-006](./08_DECISION_LOG.md).

### 사소한 결함 (코드) — 2026-08-30 갱신

- ~~`.gitignore`가 `data/views.json`을 무시하는데 실제 파일명은 `data/events.jsonl`~~
  → **해결됨.** `.gitignore`를 `data/events.jsonl`로 수정.
- ~~`node_modules`가 없어 빌드·실행이 검증된 적이 없다~~
  → **해결됨.** Node v24.19.0 설치, `npm install` 완료(next 15.5.24 / react 19.2.8 / tailwind 4.3.3),
  `npm run build` 통과(타입 에러 0, 10개 페이지 정적 생성), 브라우저에서 실제 동작 확인.
- ~~아직 git 저장소가 아니다~~ → **해결됨.** `git init` + 커밋 4건 (2026-08-31)
- `src/app/api/view/` 빈 디렉터리가 남아 있다. 실제 엔드포인트는 `src/app/api/log/`.

## 2026-08-31 오후 작업분 — 발표용 화면 완성

| | |
|---|---|
| 데이터 모델 v2 | Step 공통화 / `goodImage`·`badImage` / `Recipe` / `PrepTask` |
| 시드 교체 | 버거집 → **오픈조·마감조·제빵 + 레시피 10 + 프렙 4목록** (중간 점검·오후·마감 준비·주기) (숫자 정본: [deliverables/21_화면명세.md §1-b](deliverables/21_화면명세.md)) |
| **프렙 `/prep/[slug]`** | 리드타임 계산("지금 걸면 → 내일 07:43"), `recoverable`로 경고 분리, 배수 |
| **근무 스케줄** | 현재 시각의 근무조를 찾아 그 조의 화면을 먼저 띄움 |
| **레시피 `/r`, `/r/[slug]`** | 이름으로 찾기 + 배수 계산, `noindex`로 검색 차단 |
| 레시피 수치 | 지어낸 값 → **업계 표준값** (에스프레소 1:2 / 베이커스 퍼센트) |

브라우저에서 직접 눌러 확인: 프렙 체크(0/5→1/5), 배수(식빵 6→9개, 상세 6→12개),
레시피 검색("라떼"), 스케줄 현재 시각 인식, 월말 날짜(8/31→"내일") 버그 수정.

> **`recoverable` 필드가 이 작업의 핵심이다.** 사용자 확인(쿠팡에서 웬만한 건 다 산다)에서
> 나왔다. 발주는 깜빡해도 돈으로 메우지만 콜드브루·반죽·르방은 못 되돌린다.
> 전부 빨간 불로 띄우면 사람은 무시하므로, 경고는 되돌릴 수 없는 것에만 준다.

## 다음 할 일

**2026-08-31부터 트랙이 둘이다.** 발표 일정이 정해지면 A가 우선한다.

### A. 사업화 지원 — ★ **9/17 18:00 서류 마감** ([D-008] · 날짜 정정 [D-011])

> ★ **마감은 9/18 이 아니라 9/17(목) 18:00 이다.** 데모데이는 최종점수의 **30%** 일 뿐이고,
> 승부는 **9/22 계획서 심사(70%)** 에서 난다. 발표는 **5분 · 7분 두 벌**이고 13분이 아니다.
> **지역 연계 25점 · 활용계획 10점**은 아직 백지다.
> 근거는 공고문 원문 19쪽 분석이고 요약은 [D-011](08_DECISION_LOG.md) 에 있다.
> **사업화 지원 서류는 저장소에 두지 않는다 — 사장님께 파일로 전달했다.**

대본·Q&A 방어·실행 목록은 별도 문서로 작성됨 (Claude Artifact, 2026-08-31).

1. **경험을 숫자로 만든다** — 위 '진행 중' 참조. 앉아서 30분. 발표 전체의 뼈대
2. ~~배포 결정~~ → **결정됨.** 시연용 제한 배포 ([D-007]). 실제 배포 작업은 남아 있다
3. ~~시드를 카페·베이커리로 교체~~ → **완료**
4. **촬영 모드 + 실증 매장에서 사진·영상 확보** — 지금 회색 플레이스홀더라 화면이 비어 보인다
5. **매장 투입** — "만들었습니다"와 "쓰고 있습니다"의 차이. 가장 강한 카드
6. **점주 5명에게 금액을 직접 묻는다** — B4를 메우는 유일한 경로. Q&A에서 반드시 찔린다
7. 실증 매장 2곳을 **구두 약속 → 참여 확인서**로. "확보"와 "확약"은 다르게 읽힌다
8. PIN 잠금 (레시피는 영업비밀. 지금은 `noindex`만)

### B. 제품

1. **`docs/day-flow.md` 남은 확인 항목** — 평일 주문 마감 시각, 교대 인원,
   A/B/C 리드타임 목록 중 우리 매장에 없는 것·빠진 것
2. **레시피 수치를 매장 기준으로 덮어쓰기** — 지금은 업계 표준값이다.
   심사위원이 물어볼 숫자이므로 발표 전에 한 번은 훑어야 한다
3. ~~데이터 모델 정리~~ → **완료**

> ~~주방 관계자 3명 인터뷰~~ → [D-004]로 요구 해제. **다만 [D-006]으로 상황이 바뀌었다** —
> 실증 가능한 매장이 1~2곳 있으므로, 인터뷰가 아니라 **실제 도입 후 측정**으로 바로 갈 수 있다.

## 문서 목록

| 파일 | 내용 |
|---|---|
| `00_PROJECT_CURRENT.md` | 이 문서 |
| `00_아이디어_원문.md` | 최초 아이디어와 가설 목록 |
| `01_문제정의.md` | 문제 정의 + 검증 설계 |
| `08_DECISION_LOG.md` | 의사결정 이력 |
| `배포.md` | 배포 시 누르는 순서 |
| `../README.md` | 코드 실행·구조 (개발자용) |
