# 배포 — 사장님이 눌러야 하는 것

> 2026-09-10. **D-007(배포 여부)이 "배포한다"로 확정됐다.**
> 근거: 과정 공고문이 지원 항목으로 *"프로토타입을 **실서비스 수준으로 고도화,
> 클라우드 배포 환경 구축**"* 을 명시하고, **9/12 커리큘럼에 "서비스 배포 환경 구축"** 이
> 박혀 있다. Demo Day(9/18)에서 로컬 서버로 시연하면 그 자체가 감점이다.
>
> **저(클로드)는 배포를 못 합니다.** Vercel CLI도 토큰도 없고 `vercel login` 은
> 사장님 계정의 브라우저 인증이 필요합니다. 아래는 **사장님이 누르는 순서**입니다.

---

## 0. 지금 상태 — 배포 게이트는 다 닫혔다

`01_MVP기획서.md §10.3` 의 (나)(다) 항목을 실제 코드로 다시 확인한 결과다.

| # | 조치 | 상태 |
|---|---|---|
| 7 | D-007 배포 여부 확정 | ✅ **확정 (2026-09-10, 사장님)** |
| 8 | `public/app.html` 빼기 | ✅ 완료 |
| 9 | noindex | ✅ `layout.tsx` 전역 |
| 10 | `robots.txt` | ✅ `Disallow: /` |
| 11 | 매장 PIN | ✅ `/r` 계열에 `StoreGate` |
| 18 | `/roster` 잠금 | ✅ `OwnerGate` |
| 16 | 출퇴근·계약 내보내기 | ✅ `/backup` (2026-09-10 백업 v2로 점검 기록까지) |
| **17** | **잠금번호 재설정 경로** | ✅ **2026-09-10 완료** |
| 13 | `og:url` / `NEXT_PUBLIC_SITE_URL` | 🟠 **자리는 만들어뒀다.** 아래 3번에서 값을 넣는다 |
| 14 | 보관 기간·삭제 요청 처리 | ⛔ **미정 (사장님).** 처리방침의 빈칸. 배포는 막지 않는다 |

**시드에 개인정보가 0건인 것을 확인했다** — 실명·전화·이메일·시급 모두 `0`.
직원 이름·시급·매출은 **각자 브라우저에만** 쌓이므로, 배포해도 남의 정보가 딸려가지 않는다.

⚠️ **레시피 수치는 아직 플레이스홀더(표준 비율)다.** 그래서 지금 배포해도 새어 나갈
영업비밀이 없다. **실매장 수치를 넣는 순간 사정이 달라진다** — 매장 PIN 은 가림막이고,
서버가 HTML 에 재료를 실어 보낸다(실측 2026-09-07). 그때는 서버 인증이 먼저다.

---

## 1. main 에 머지 (GitHub Flow)

작업이 전부 **`claude/musing-hermann-db7d28`** 에 있고 **main 보다 44 커밋 앞서 있다.**
(`claude/handoff-docs-review-8988e3` 는 5 커밋짜리 옛 가지다 — **이쪽이 아니다.**)
Vercel 은 기본으로 main 을 배포하므로 머지가 먼저다.

**PR 만들기** — 아래 주소를 열면 바로 PR 화면이 뜬다.

```
https://github.com/JJ0829/store-note/compare/main...claude/musing-hermann-db7d28
```

1. `Create pull request` → 제목 아무거나 → `Create`
2. `Merge pull request` → `Confirm merge`

> 테스트는 전부 통과한 상태다(**425개**, 타입 오류 0, 프로덕션 빌드 통과).
> CI 는 아직 없어서 자동 검사는 안 돈다.

---

## 2. Vercel 에 올리기 (3분)

1. <https://vercel.com> 로그인 → **Add New… → Project**
2. **Import Git Repository** → GitHub 연결 → `JJ0829/store-note` 선택
   - 비공개 저장소라 **GitHub 앱 권한 허용**을 한 번 물어본다
3. 설정은 **건드릴 것이 없다** — Next.js 를 자동으로 알아본다
   - Framework: `Next.js` / Build: `next build` / Root: `./`
4. **Deploy**

끝나면 `store-note-xxxx.vercel.app` 같은 주소가 나온다.

---

## 3. 주소를 환경변수에 넣고 한 번 더 배포

카카오톡으로 체크리스트 링크를 보내는 것이 이 제품의 실제 경로인데,
주소를 안 넣으면 **미리보기가 안 뜬다.**

1. Vercel 프로젝트 → **Settings → Environment Variables**
2. 추가:

| Key | Value |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | 방금 받은 주소 (예: `https://store-note-xxxx.vercel.app`) |
| `ANTHROPIC_API_KEY` | <https://console.anthropic.com> 에서 발급 (`sk-ant-…`) |

⚠️ **`ANTHROPIC_API_KEY` 에는 `NEXT_PUBLIC_` 을 붙이면 안 된다.** 붙이면 키가
브라우저 파일에 그대로 박혀서 누구나 꺼내 쓸 수 있다. 지금 코드는 서버에서만 읽는다.

> 키를 안 넣어도 나머지 화면은 전부 돈다. **촬영 목록 만들기만**
> "AI 키가 설정되지 않았습니다" 라고 뜬다.

3. **Deployments → 맨 위 → ⋯ → Redeploy**

> **빌드 시점에 박히는 값이라 재배포가 필요하다.** 나중에 주소를 바꿔도 마찬가지.

---

## 4. 배포 직후 확인 (5분)

| 확인 | 어떻게 | 기대 |
|---|---|---|
| 검색 차단 | `주소/robots.txt` | `Disallow: /` |
| 첫 화면 | `주소/` | 지금 시각의 근무조가 뜬다 |
| 프렙 | `주소/prep` | 목록 3개 + 레시피 찾기 (매장 번호를 묻는다) |
| 프렙 상세 | `주소/prep/afternoon` | 카드 5개, `지금 걸면 → …부터 사용 가능` |
| 레시피 잠금 | `주소/r` | 매장 번호를 묻는다 |
| 사장님 잠금 | `주소/sales` | 사장님 번호를 묻는다 + `번호를 잊으셨나요?` |
| 404 | `주소/없는주소` | 404 화면 + `처음으로` |
| **AI** | `주소/shoot` 맨 아래에 `아인슈페너` 넣고 누르기 | 찍을 목록 8~16개 |
| **폰으로** | 태블릿·폰에서 같은 주소 | 카톡으로 링크 보내서 열어보기 |

⚠️ **지표(`/api/log`)는 서버 파일(`data/events.jsonl`)에 쓴다.**
Vercel 은 파일 시스템이 배포마다 초기화되므로 **배포본에서는 지표가 안 쌓인다.**
지금은 깨지지 않고 조용히 버려지기만 한다 — Supabase 를 붙일 때 같이 옮긴다.

---

## 5. 배포하면 달라지는 것 — 알고 있어야 할 것

- **주소를 아는 사람은 누구나 연다.** 검색에는 안 걸리지만(noindex + robots) 링크는 열린다.
  체크리스트를 카톡으로 돌리는 것이 원래 설계이므로 이건 의도된 것이다.
- **레시피는 매장 번호로 가려지지만 그건 가림막이다.** 페이지 소스에 재료가 들어 있다.
  **실매장 레시피 수치를 넣기 전까지만 안전하다.**
- **데이터는 여전히 각 브라우저에만 있다.** 태블릿에서 넣은 출퇴근이 폰에서 안 보인다.
  기기가 바뀌면 `/backup` 으로 옮겨야 한다. 이게 Supabase 가 필요한 이유다.

---

## 관련

- 배포 전 조치 정본: [deliverables/01_MVP기획서.md §10.3](deliverables/01_MVP기획서.md)
- 결정 로그: [08_DECISION_LOG.md](08_DECISION_LOG.md) D-007
- 화면·상태 기준: [deliverables/21_화면명세.md](deliverables/21_화면명세.md)
