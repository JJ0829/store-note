# 21. 화면 명세 — 화면 · 상태 · 이동

> **이 문서가 정하는 것:** 다음 개발 단계에서 쓸 **화면·상태·이동의 기준**.
> **이 문서가 정하지 않는 것:** 데이터베이스, 배포, 서버.
>
> 2026-09-09 작성. [20_기능흐름도.md](20_기능흐름도.md)의 흐름을 화면 단위로 내린 것이다.
> [02_화면설계서.md](02_화면설계서.md)는 **이미 만들어진 화면을 감사한 기록**이고,
> 이 문서는 **다음에 만들 것의 기준**이다. 둘이 어긋나면 이 문서가 최신이다.
>
> ⚠️ 아래 표의 `상태` 칸은 **코드에서 읽어낸 실제 값**이다. 추측이 아니다.
> 다만 "있어야 하는데 없는 것"은 ⛔ 로 따로 표시했다 — 그건 **명세이지 현황이 아니다.**

---

## 0. 오늘 확정한 것 — 세 줄

1. **화면은 19개, 그중 매일 열리는 것은 6개다.** 나머지 13개는 설정·기록·관리다.
2. **모든 하루 단위 화면의 날짜는 `businessDay()`(새벽 4시 경계)다.** 달력 날짜 금지.
3. **잠금은 세 겹이다** — 안 잠김 / 매장 PIN(레시피) / 사장님 PIN(돈·사람).
   그리고 **셋 다 접근 통제가 아니라 가림막이다.** 서버가 생기기 전까지는 그렇다.

---

## 1. 화면 목록 (19)

`매일`은 CLAUDE.md의 "매일 열리는 이유" 기준이다. 이게 이 제품이 살아남는 축이다.

| # | 경로 | 이름 | 누가 | 매일 | 잠금 |
|---|---|---|---|---|---|
| **S-01** | `/` | 오늘 | 전원 (공용 태블릿) | ✅ | 없음 (부분 가림) |
| **S-02** | `/t/[slug]` | 교육 모드 | 신입 + 선배 | ✕ 첫날만 | 없음 |
| **S-03** | `/p/[slug]` | 체크리스트 | 직원 | ✅ | 없음 |
| **S-04** | `/prep/[slug]` | 프렙 | 오픈조·마감조·제빵 | ✅ | 없음 |
| **S-05** | `/r` | 레시피 찾기 | 전원 | ✅ | 🔑 매장 |
| **S-19** | **`/prep`** | **프렙 목록 + 레시피 찾기** (탭에서 들어온다) | 전원 | ✅ | 🔑 매장 |
| **S-06** | `/r/[slug]` | 레시피 1건 | 전원 | ✅ | 🔑 매장 |
| **S-07** | `/r/new` | 레시피 추가 | 사장·주방장 | ✕ | 🔑 매장 |
| **S-08** | `/r/my` | 내가 추가한 레시피 | 사장·주방장 | ✕ | 🔑 매장 |
| **S-09** | `/attendance` | 출퇴근 · 근태 | 전원 | ✅ 하루 2번 | 부분 (인건비만) |
| **S-10** | `/roster` | 근무표 | 사장 | ✕ 주 1회 | 🔒 사장님 |
| **S-11** | `/order` | 발주 | 오픈조 | ✅ 아침 | 없음 |
| **S-12** | `/vendors` | 거래처 · 단가 | 사장 | ✕ | 🔒 사장님 |
| **S-13** | `/cost` | 원가 | 사장 | ✕ | 🔒 사장님 |
| **S-14** | `/sales` | 매출 · 남은 돈 | 사장 | ✅ 마감 | 🔒 사장님 |
| **S-15** | `/contracts` | 근로계약서 | 사장 | ✕ | 🔒 사장님 |
| **S-16** | `/backup` | 내보내기 · 되돌리기 | 사장 | ✕ | 🔒 사장님 |
| **S-17** | `/shoot` | 촬영 진행 | 사장 | ✕ | 없음 |
| **S-18** | `/(없는 주소)` | 404 | — | — | 없음 |

**매일 열리는 6개: S-01 · S-03 · S-04 · S-19 · S-09 · S-11** (+ 마감에 S-14).
**하단 탭바가 그중 다섯을 그대로 태운다** — `오늘(S-01) · 프렙(S-19) · 발주(S-11) · 출퇴근(S-09) · 운영`.
기능 추가 요청이 오면 **이 여섯을 늘리는지 먼저 따진다.**

---

## 1-b. 숫자 정본 ★ 다른 문서는 여기를 가리킨다

> **왜 여기 하나에 모았나.** 같은 숫자가 아홉 문서에 흩어져 있었고,
> 시드를 고칠 때마다 전부 낡았다. 2026-09-10 점검에서 **28군데가 틀린 채로**
> 남아 있는 것을 발견했다. 틀린 숫자가 적힌 문서는 안 읽는 것만 못하다 —
> 심사장에서 화면과 문서가 다르면 그 자리에서 신뢰를 잃는다.
>
> **아래 값은 `tests/seed.test.ts` 의 "★ 숫자 정본" 테스트가 못 박는다.**
> 시드를 고치면 그 테스트가 깨지고, 깨지면 **이 표를 같이 고치라는 뜻**이다.
> 다른 문서에 숫자를 다시 적지 말 것. 여기를 링크할 것.

**기준: `data/seed.json` · 2026-09-10**

| 무엇 | 전체 | 화면에 세는 수 | 되돌릴 수 없음 |
|---|---|---|---|
| 오후 프렙 `afternoon` | 11 | **5** (+ 옵션 6) | **7** |
| 마감 준비 `evening` | 3 | **3** | 0 |
| 주기 점검 `cycle` | 16 | **13** (묶음 머리 3 제외) | 1 |
| **프렙 합계** | **30** | — | **8** |

| 무엇 | 수 |
|---|---|
| 레시피 | **10** (스텝 32) |
| 포지션 | **3** — 오픈조 9 · 마감조 9 · 제빵 8 = 스텝 **26** |
| 근무조 | **4** |
| 레시피가 붙은 프렙 항목 | **8** (콜드브루·반죽·르방·청·냉침차·밀크티·시럽·크림폼) |
| 수량이 매일 바뀌는 항목 `quantityVaries` | **10** |
| 화면 | **19** (§1) |
| 촬영 대상 `/shoot` | **88** = 포지션 26 + 레시피 32 + 프렙 30 |

### 두 숫자가 다른 것은 일부러다 — 헷갈리지 말 것

- **`전체`** 는 `list.tasks.length` 다. 시드에 든 줄 수.
- **`화면에 세는 수`** 는 `repo.countedTasks()` 다. **옵션과 묶음 머리를 뺀다.**
  르방을 안 쓰는 매장에서 `9/10` 이 영영 안 채워지면 아무도 진행률을 안 본다.
- **빨간 안내(되돌릴 수 없음)는 옵션까지 센다.** 르방을 *쓰는* 매장에서
  "다 했습니다" 가 거짓이 되면 안 되기 때문이다. → CLAUDE.md `optionOf` / `optional`

⚠️ **촬영 88 에는 묶음 머리 3개가 섞여 있다.** 찍을 것이 아니라 이름표인데
`/shoot` 이 `l.tasks` 를 그대로 돈다. 2026-09-10 점검에서 확인된 **미해결**이다 (§7).

---

## 2. 상태 정의 — 모든 화면이 이 여섯 중에서만 고른다

화면마다 다른 말을 쓰면 만드는 사람도 쓰는 사람도 헷갈린다. 여섯으로 고정한다.

| 상태 | 언제 | 무엇을 보여주나 | 코드 표식 |
|---|---|---|---|
| **L 로딩** | `localStorage` 를 아직 못 읽음 (SSR → 클라이언트 사이) | 회색 네모 (`animate-pulse`) | `if (!now \|\| !data) return <skeleton>` |
| **N 정상** | 데이터가 있다 | 본문 | — |
| **Z 비었음** | 데이터가 0건 | **다음에 뭘 할지 알려주는 문장 + 버튼** | `<Empty>` |
| **X 오류** | 저장 실패 · 형식이 안 맞음 | 빨간 글, `role="alert"` | `role="alert"` |
| **G 잠김** | PIN 안 풀림 | PIN 입력칸만 | `OwnerGate` / `StoreGate` |
| **C 주의** | 정상인데 **믿으면 안 되는 값** | 회색 작은 글 | `<Caveat>` |

**C(주의)가 이 제품에 특히 많은 이유:** 인건비는 추정이고(4대보험·소득세 없음),
원가는 단가를 못 구한 재료가 있으면 낮게 나온다. **숫자를 보여주되 그 숫자를
얼마나 믿어도 되는지 같이 보여준다.** 이게 없으면 사장님이 틀린 숫자로 가격을 정한다.

### 상태 현황 — 실측 (2026-09-09)

| 화면 | L | Z | X | C | 비고 |
|---|---|---|---|---|---|
| S-01 오늘 | ✅ (NowPanel) | — | — | — | 시각을 클라이언트에서 읽으므로 로딩이 필요 |
| S-02 교육 모드 | — | — | — | — | 서버 데이터만 쓴다. 진도는 sessionStorage |
| S-03 체크리스트 | — | — | — | — | 서버 데이터만 쓴다 |
| S-04 프렙 | — | — | — | — | 서버 데이터만 쓴다 |
| S-09 출퇴근 | ✅ | ✅ | ⛔ **없음** | ✅ ×2 | 성공만 알리고 실패는 조용하다 (§7-①) |
| S-11 발주 | ✅ | ✅ | ⛔ **없음** | ✅ | |
| S-12 거래처 | ✅ | ✅ | ⛔ **없음** | — | |
| S-13 원가 | ✅ | ✅ | — | ✅ ×2 | |
| S-14 매출 | ✅ | — | — | ✅ ×3 | |
| S-15 계약서 | ✅ | ✅ | — | ✅ | |
| S-16 백업 | ✅ | — | ✅ ×2 | ✅ ×3 | 되돌리기가 파괴적이라 가장 두껍다 |
| S-10 근무표 | ✅ | — | — | — | |
| 잠금 화면 | ✅ | — | ✅ | — | PIN 틀림을 `role="alert"` 로 읽어준다 |

---

## 3. 핵심 화면 5개 — 상세

고른 기준: **매일 열린다 + 발표 시연 동선에 있다.**

### S-01 `/` 오늘 — 허브

| | |
|---|---|
| 목적 | **메뉴를 뒤지지 않게 한다.** 지금 시각에 근무 중인 조와, 그 조가 열 화면만 위에 올린다 |
| 진입 | 태블릿 첫 화면 / `‹ 뒤로` 의 폴백 / `처음으로` (⚠️ **3개 화면에만 있다** — §4-b) |
| 데이터 | `seed.shifts` (서버) + `new Date()` (클라이언트) |
| 날짜 | — (시각만 씀) |

**상태**

| | 조건 | 화면 |
|---|---|---|
| L | 클라이언트 시각 읽기 전 | 회색 네모 |
| N-a | 근무 중인 조가 1개 이상 | `지금 HH:MM · 근무 중` + 조 카드들. **방금 시작한 조가 맨 위** |
| N-b | 근무 중인 조가 없음 (22:30~05:00) | `근무 시간이 아닙니다` + **다음 조** |

**이동** — 각 조의 `focus[]` 순서대로 버튼이 뜬다. 이게 이 화면의 전부다.

| 조 | 시각 | 버튼 |
|---|---|---|
| 제빵 | 05:00~13:00 | 제빵 순서 보기 `/t/bakery-morning` → 레시피 찾기 `/r` |
| 오픈조 | 07:30~15:30 | 오픈 체크리스트 `/p/cafe-open` → 오후 프렙 `/prep/afternoon` |
| 미들 | 11:00~19:00 | 레시피 찾기 `/r` |
| 마감조 | 14:30~22:30 | 오후 프렙 `/prep/afternoon` → **마감 준비 `/prep/evening`** → 마감 체크리스트 `/p/cafe-close` |

⚠️ **자정을 넘는 조가 있으면 `shiftClock.isOnDuty()` 로 판정해야 한다.**
평소 마감조는 22:30 에 끝나지만 **01:00 퇴근이 예외로 있다.** 그날 근무조를
`14:30~01:00` 으로 잡으면 옛 판정(`cur >= start && cur < end`)에서 **통째로 사라진다.**

---

### S-04 `/prep/[slug]` 프렙 — 제품이 종이를 이긴다고 주장하는 화면

| | |
|---|---|
| 목적 | **오늘 안 하면 내일 아침에 터지는 것**을 오늘 눈에 보이게 한다 |
| 진입 | S-01 프렙 행 3개 / 근무조 `focus` |
| 데이터 | `seed.prepLists` (서버) + 체크 상태 `prep:{slug}:{영업일}` |
| 날짜 | **`businessDay()`** — 자정이 아니라 새벽 4시에 바뀐다 |
| 경로 | `/prep/afternoon` · `/prep/evening` · `/prep/cycle` (3개, 정적 생성) |

**화면 순서 — 위에서 아래가 곧 중요도다**

1. 진행률 막대 + `n/전체`
2. 🔴 **`recoverable:false` 안내** — "까먹지 말고 해야 할 것 n개". 다 하면 초록으로 바뀐다
3. 목록 안내문 (`list.note`)
4. 항목 카드들

**항목 카드 안에서 조건부로 뜨는 것**

| 조건 | 표시 |
|---|---|
| `critical` | 🏷 `꼭 지키기` |
| `quantityVaries` | 🏷 `수량 매일 다름` ← **종이로 못 하는 이유** |
| `!recoverable` | 카드에 빨간 테두리 |
| `leadTimeHours !== null` | `지금 걸면 → 내일 07:43 부터 사용 가능 (16시간)` |
| `leadTimeDays !== null` | `오늘 주문 → 목요일 도착 (주말 배송 없음)` |
| `recipeSlug !== null` | **레시피 전체** — 배수 계산기 + **만드는 순서**. 27개 중 **8건**. 옵션 안에서도 뜬다 |
| `kind === "routine"` | **리드타임 줄이 아예 안 뜬다** ← 없는 약속을 만들지 않기 위해 |
| `optionOf !== null` | **독립 카드가 아니라 부모 카드 안**에 그려진다 |
| 자식이 `optional:false` | 그 카드는 **「묶음 머리」** — 누를 수 없고 분모에도 안 들어간다 |

**두 숫자의 분모가 다르다 — 일부러다 (2026-09-09)**

| 숫자 | 무엇을 세나 | 옵션(`optionOf`)을 |
|---|---|---|
| `n/전체` · 진행률 막대 | 오늘 이 목록에서 할 일 | ⛔ **뺀다** |
| 「까먹지 말고 해야 할 것 n개」 | `recoverable:false` 인 것 | ✅ **센다** |

**왜 나눴나.** 매장마다 하거나 안 하는 일이 있다 — 사워도우를 안 하는 카페에
르방은 뜻이 없다. 그게 분모에 들어가면 **`9/10` 이 영영 안 채워지고 진행률이
거짓이 된다.** 그러면 사람은 곧 진행률을 안 본다.

**그리고 옵션은 흩어놓지 않는다** (사장님 지적: *"따로말고 한곳에 모아서 해줘"*).
바에서 만드는 부재료 다섯은 **`바 부재료 점검 · 제조` 한 카드**에 모여 있다.
그래서 오후 프렙은 **카드 5개 + 옵션 6개**다 — 카드 11개가 아니다.

### 주기 점검도 같은 방식으로 묶었다 (2026-09-08 요청)

13개가 한 줄로 늘어서 있어서, 저울 영점(**7일**)과 소화기 점검(**1년**)이 나란히
있었다. 세 묶음으로 나누고 **묶음 안은 주기가 짧은 것부터** 세웠다.

| 묶음 (누를 수 없다) | 안에 든 것 |
|---|---|
| **기계 · 설비 점검** | 저울 영점 7일 · 정수 필터 4개월 · 개스킷 6개월 · 그라인더 날 6개월 · 냉장 가스켓 6개월 · 머신 정기점검 1년 |
| **안 보이는 곳 청소** | 배수구·트렌치 7일 · 제빙기 1개월 · 방충·방역 1개월 · 후드·덕트 3개월 🔴 |
| **서류 · 법정** | 보건증 1년 · 소화기·소방 1년 · 위생교육 1년 |

**진행률은 `n/13` 이다 — 16이 아니다.** 묶음 머리는 할 일이 아니라 이름표라서
누를 수 없고 분모에도 안 들어간다. 부모까지 세면 같은 일을 두 번 센다.

**그리고 이건 옵션이 아니다.** 주기 점검에 *"매장에 따라 안 하기도 합니다"* 를
붙이면 거짓이다 — 보건증·소방·위생교육은 안 하면 과태료다. 그래서 같은 자리에
`이 묶음의 항목 n개` 를 쓴다. (`tests/seed.test.ts` 가 `optional: false` 를 못 박는다)

### 오후 프렙

| 카드 | 옵션 |
|---|---|
| 내일용 콜드브루 걸기 🔴 | — |
| 내일용 반죽 제조 후 냉장 🔴 | 르방 갱신 🔴 |
| **바 부재료 점검 · 제조** | 에이드 청 🔴 · 냉침차 🔴 · 밀크티 베이스 🔴 · 시럽 · 크림폼 |
| 우유·식자재 발주 | — |
| 원두 발주 🔴 | — |

🔴 = `recoverable:false`. **일곱 개인데 진행률 분모는 다섯이다.** 위의 표가 그 이유다.

그런데 빨간 안내에서도 빼면 **르방을 쓰는 매장에서 "다 했습니다"가 거짓이 된다.**
그건 이 제품이 파는 것(누락 방지) 자체를 무너뜨린다. 그래서 안내에는 센다.

실측: 오후 프렙에서 옵션을 체크하면 `0/9` 는 그대로, 안내는 `7개 → 6개`.

**상태**

| | 조건 | 화면 |
|---|---|---|
| N | 항상 | 서버가 목록을 실어 보내므로 로딩이 없다 |
| Z | ⛔ **없음** | 목록이 비는 경로가 지금은 없다. 사용자가 목록을 만들게 되면 필요해진다 |
| — | 영업일이 바뀜 | 지난 키를 지우고(`pruneDayKeys`) 체크가 초기화된다 |

### 프렙 안에 레시피가 통째로 들어왔다 (2026-09-10)

사장님 요청: *"프렙이랑 레시피랑 같이 있어야 하는데 합쳐줘."*

전에는 프렙에 **재료와 배수만** 있었다. 만드는 순서를 보려면 `/r/{slug}` 로
나가야 했고, **그 화면은 매장 PIN 이 걸려 있다.** 주방에서 크림폼을 올리다 말고
화면을 나갔다가, 번호를 넣고, 다시 프렙으로 돌아와 체크해야 했다.
**젖은 손으로 네 번이다.** 재료와 순서는 한 몸인데 나눠 놨던 것이다.

- `만드는 순서 n단계 ▾` 를 누르면 그 자리에서 펼쳐진다 (기본은 접힘)
- 접어 두는 이유: 오후 프렙에만 레시피 붙은 항목이 여덟이다. 전부 펼치면
  목록을 못 훑는다. **한 번 누르는 것과 화면을 옮기는 것은 다르다** —
  번호도 안 묻고 보던 자리를 잃지도 않는다
- 사진 자리는 레시피 화면과 **같은 id**(`step.id`)를 쓴다. 한 번 찍어 넣으면 두 화면에 뜬다

⚠️ **매장 PIN 이 더 얇아졌다.** `/prep/` 은 잠금이 없는데 이제 재료·중량뿐 아니라
**만드는 순서까지** 거기 있다. 즉 매장 PIN 은 실질적으로 `/r` 화면 하나만 가린다.
다만 재료와 중량은 **이전에도 프렙에 그대로 있었고**, 서버가 HTML 에 실어 보내므로
잠긴 상태에서도 페이지 소스에 들어 있었다(§4 실측). **원래부터 가림막이었다.**
`/prep/` 은 카톡으로 공유하는 주소가 아니라 태블릿에서만 여는 화면이라
새로 생긴 유출 경로는 없다 — 그래도 **진짜 잠금은 서버가 생긴 뒤**라는 사실은 그대로다.

**화면이 하는 약속:** *"체크 상태는 이 기기에만 저장되며 영업일이 바뀌면
초기화됩니다. 하루의 경계는 새벽 4시입니다."* — 이 문장이 참이 되도록
`pruneDayKeys` 가 지난 날짜 키를 실제로 지운다. **약속만 하고 안 지우던 버그였다.**

---

### S-03 `/p/[slug]` 체크리스트 — 매일 쓰는 것

| | |
|---|---|
| 목적 | 혼자 일하다 확인. **교육이 아니라 반복**이다 |
| 진입 | S-01 포지션 카드 `체크리스트` / 카카오톡 링크 / 근무조 `focus` |
| 데이터 | `seed.positions` (서버) + `sop:{shareSlug}:{영업일}` |
| 날짜 | **`businessDay()`** |
| 경로 | `/p/cafe-open` · `/p/cafe-close` · `/p/bakery-morning` |

**S-02 교육 모드와 무엇이 다른가 — 이게 두 화면을 나눈 이유다**

| | S-02 교육 모드 `/t/` | S-03 체크리스트 `/p/` |
|---|---|---|
| 보여주기 | **한 장씩** 넘긴다 | 전부 스크롤 |
| 저장 | `sessionStorage` + `runId` | `localStorage` + 영업일 |
| 왜 | **공용 태블릿이라 앞사람 진도가 다음 신입에게 보이면 안 된다** | 그날 진도는 남아야 한다 |
| 필수 항목 | **확인 버튼을 눌러야 넘어간다** | 그냥 체크 |
| 끝나면 | 완료 설문 (`선배에게 몇 번 물어봤나`) | 없음 |
| 지표 | `training_start` · `critical_confirm` · `training_complete` · `survey` | `view` |

---

### S-11 `/order` 발주 — 아침에 여는 순서가 곧 화면 순서

| | |
|---|---|
| 목적 | **주문했는데 안 들어온 것**을 아침에 잡는다. 종이가 못 잡는 게 정확히 여기다 |
| 진입 | S-01 `발주` |
| 데이터 | `seed.prepLists` 의 `kind:"order"` + `sop:vendors` + `sop:orderLog` + `sop:orderLinks` |
| 날짜 | **`businessDay()`** + 지난 7영업일 (`recentDays`) |

**섹션 순서 = 아침 순서**

| # | 섹션 | 조건 | 없으면 |
|---|---|---|---|
| 1 | 🔴 **주문했는데 안 들어온 것** | 지난 7영업일에 `ordered && !received` | 섹션 자체가 안 뜬다 |
| 2 | ⏰ **오늘 주문 마감** | 거래처가 1건 이상 | 섹션 자체가 안 뜬다 |
| 3 | 📝 **주문할 것** | 항상 | `Z` — "프렙 목록에 발주 항목이 없습니다" |

**"주문함 / 들어옴" 두 칸으로 나눈 게 이 화면의 핵심이다.**
종이 체크리스트는 "주문함"까지만 적는다. 사고는 그 다음 칸에서 난다.

⛔ **§6 의 결정이 이 화면 2번 섹션을 바꾼다.** 지금은 **모든 거래처를 매일** 세운다.

---

### S-09 `/attendance` 출퇴근 · 근태 — 가장 자주 눌리는 화면

| | |
|---|---|
| 목적 | 버튼 두 개. 그리고 **근무표(계획)와의 차이**가 근태가 된다 |
| 진입 | S-01 `출퇴근 · 근태` |
| 데이터 | `sop:punch` + `sop:roster` + `sop:contracts` + `sop:settings` |
| 잠금 | **부분** — 화면은 열려 있고 **시급·인건비만** `InlineUnlock` 으로 가린다 |

**왜 화면 전체를 안 잠갔나:** 출퇴근은 직원이 찍는다. 전체를 잠그면 직원이
매번 사장님을 불러야 한다. 그런데 시급은 옆 사람이 보면 안 된다. 그래서 반만 잠근다.

**C(주의) 두 개 — 반드시 화면에 남는다**

- 인건비는 **추정**이다. 4대보험·소득세·수습감액·연차수당이 없다. **급여 대장이 아니다**
- 5인 미만이면 연장 가산수당이 없다 (근로기준법 제11조) → `settings.fiveOrMore`

⚠️ **자정을 넘는 퇴근** — `23:00 → 01:00` 을 그냥 빼면 −22시간이고 인건비가 깎인다.
`attendance.workedMinutes()` 가 처리한다.

---

## 4-b. 이동 규칙 — 홈으로 가는 길 ⚠️ **오늘 정함**

| 장치 | 어디에 | 동작 |
|---|---|---|
| `‹ 뒤로` | `Screen` 을 쓰는 **모든 화면** | `history.length > 1` 이면 `router.back()`, 아니면 `/` 로 |
| `처음으로` | ⚠️ **`/r` · 프렙 · 교육 모드 3곳뿐** | `/` 로 |
| 하단 탭바 | ✅ **두 앱 모두** (2026-09-10). `오늘 · 프렙 · 발주 · 출퇴근 · 운영` | 교육 모드·체크리스트에서는 안 그린다 |

**왜 이게 문제인가:** 매장 태블릿은 **전체화면 키오스크라 주소창이 없는 경우가 많다**
(`BackButton.tsx` 주석에 이미 적혀 있다). 그 상태에서 `/attendance` → `/sales` 처럼
두세 번 들어가면 **`‹ 뒤로` 를 그만큼 눌러야 나온다.**

**기준: 하단 탭바를 표준으로 삼는다.** → ✅ **2026-09-10 두 앱 모두 구현.**

★ **탭은 매일 누르는 것만 둔다.** 옛 6칸(`오늘·프렙·레시피·근무표·교육·운영`)은
근무표(주 1회)·교육(신입 올 때만)이 탭에 있고 **하루 두 번 찍는 출퇴근과
매일 아침 여는 발주가 탭에 없었다.** 매일 누르는 게 탭에 없고 한 달에 한 번
쓰는 게 탭에 있었던 것이다 (CLAUDE.md — *"매일 열리는 이유를 늘리는지 먼저 따질 것"*).

| 탭 | 경로 | 켜지는 하위 화면 |
|---|---|---|
| 오늘 | `/` | — |
| **프렙** | `/prep` | `/prep/*` · `/r/*` |
| 발주 | `/order` | — |
| 출퇴근 | `/attendance` | — |
| 운영 | `/sales` | `/cost` · `/vendors` · `/contracts` · `/roster` · `/backup` |

**프렙과 레시피를 `프렙` 탭 하나로 합쳤다** (사장님 지적: *"아이콘이 왜 아직
프렙, 레시피 따로 있노"*). 프렙 항목 안에 레시피가 통째로 들어간 뒤로 둘을
나란히 두면 무엇이 다른지 알 수 없다. 실제로 다른 것은
**오늘 할 것(프렙)** 과 **주문 받고 찾는 것(레시피)** 인데,
그건 탭 두 개가 아니라 한 화면의 두 묶음이면 된다.

⚠️ **교육 모드(`/t/`)와 체크리스트(`/p/`)에는 탭바를 안 그린다.**
ⓐ 교육 모드는 한 장씩 넘기는 **한 방향 흐름**이라 중간에 새는 문이 있으면 안 된다.
ⓑ 체크리스트는 카카오톡 링크로 **직원 개인 폰**에서 여는 화면이다 —
거기에 매출·출퇴근 탭을 띄우면 남의 매장 사람에게 문을 여는 셈이다.

---

## 4. 잠금 규칙 — 세 겹

| 겹 | 무엇을 지키나 | 키 | 열린 상태 | 화면 |
|---|---|---|---|---|
| **없음** | — | — | — | S-01·02·03·04·11·17·18 |
| 🔑 **매장 PIN** | **레시피 = 영업비밀** | `sop:storePin` | `sessionStorage` `sop:storeOpen` | S-05~08 |
| 🔒 **사장님 PIN** | 돈 · 사람 (급여·매출·단가·계약) | `sop:ownerPin` | `sessionStorage` `sop:ownerOpen` | S-10·12·13·14·15·16 |

**규칙 세 가지**

1. **사장님 PIN 이 풀려 있으면 매장 PIN 도 풀린 것으로 본다.** 한 방향이다 (역은 아니다)
2. **열린 상태는 `sessionStorage`.** 브라우저를 닫으면 다시 잠긴다 — 공용 태블릿이라서
3. **PIN 은 단방향 요약값(FNV-1a)으로만 저장한다.** 원문을 안 남긴다

### ★ 그런데 이건 접근 통제가 아니다 — 반드시 이렇게 말할 것

**서버가 HTML 에 레시피를 실어 보낸다.** 잠긴 상태에서도 **페이지 소스에 재료와
중량이 들어 있다** (2026-09-07 실측: `curl /r/shokupan | grep 강력분` 로 확인).

> 화면 문구도 그래서 **"가림막이지 잠금이 아닙니다"** 로 쓴다.
> 한때 *"매장 번호를 걸면 링크만으로는 열리지 않습니다"* 라고 썼다가 되돌렸다.
> **그건 거짓말이었다.**

진짜 접근 통제는 **서버가 생긴 뒤**다 (D-007 → Supabase).

---

## 5. 저장 키 한눈에

| 키 | 무엇 | 지워지나 | 법정 보존 |
|---|---|---|---|
| `sop:{shareSlug}:{영업일}` | 체크리스트 체크 | ✅ 영업일 바뀌면 | |
| `prep:{slug}:{영업일}` | 프렙 체크 | ✅ 영업일 바뀌면 | |
| `sop:punch` | 출퇴근 기록 | ❌ **절대** | ★ 근로기준법 제42조 **3년** |
| `sop:contracts` | 근로계약 | ❌ **절대** | ★ 제42조 3년 |
| `sop:roster` | 근무표 | ❌ | |
| `sop:vendors` | 거래처 · 단가 | ❌ | |
| `sop:orderLog` / `sop:orderLinks` | 발주 기록 | ❌ | |
| `sop:sales` | 매출 | ❌ | |
| `sop:settings` | 최저임금 · 5인이상 · 목표원가율 | ❌ | |
| `sop:recipes` | 사장님이 추가한 레시피 | ❌ | |
| **`sop:cycleDone`** | ★ **주기 점검을 마지막으로 한 날** | ❌ **절대** | 보건증·소방은 기록이 증빙 |
| **`sop:cycleEvery`** | ★ **매장이 정한 주기(일)** — 시드에는 없다 | ❌ | 안 정하면 기한을 판단하지 않는다 |
| `sop:ownerPin` / `sop:storePin` | PIN 요약값 | ❌ | |
| `sop:sid` | 익명 방문 id (지표용) | ❌ | |
| *(session)* `sop:ownerOpen` / `sop:storeOpen` | 잠금 열림 | ✅ 브라우저 닫으면 | |
| *(session)* 교육 모드 `runId` | 이번 1회분 진도 | ✅ 끝나면 | |

⚠️ **`pruneDayKeys` 는 `{접두사}{YYYY-MM-DD}` 꼴만 지운다.** `sop:punch` 는
접두사가 겹치지만 꼬리가 날짜가 아니라서 안 지워진다. **테스트로 못 박아뒀다.**

---

## 6. E — 거래처: 세 값이 어디에 있는가 ⚠️ **결정 필요**

> `day-flow.md` 가 "리드타임 기능의 핵심 입력값"으로 표시해둔 항목이다.
> **아직 0건이다.** 오늘 확인한 것은 *"그 값을 앱이 받을 자리가 있는가"* 다.

| 값 | 지금 어디에 | 사장님이 화면에서 고칠 수 있나 |
|---|---|---|
| **주문 마감 시각** | `Vendor.cutoff` (`sop:vendors`) | ✅ `/vendors` |
| **입고 소요일** | `Vendor.leadDays` | ✅ `/vendors` |
| **배송 요일** | `Vendor.deliverDays` | ✅ `/vendors` |
| **발주 요일** *(우리가 주문하는 요일)* | ⛔ **`data/seed.json` 의 프렙 트리거뿐** | ❌ **코드를 고쳐야 한다** |

### 무엇이 문제인가

**첫째, 발주 요일을 사장님이 못 고친다.** `p-5 우유·식자재 발주` 의
`trigger: {type:"weekday", days:[…], at:"15:00"}` 안에 있고, 프렙 항목을 고치는
화면이 없다. 거래처가 요일을 바꾸면 **코드를 배포해야 한다.**

**둘째, 마감 시각이 두 군데 있다.** 프렙 트리거의 `at`(15:00)과
`Vendor.cutoff`(15:00). 지금은 우연히 같지만 **한쪽만 고치면 조용히 갈린다.**

**셋째 — 이게 화면에 바로 보이는 결과다.** `/order` 의 "오늘 주문 마감" 카드는
**모든 거래처를 매일 세운다.** 오늘 주문하지 않는 거래처까지 카운트다운이 돈다.
거래처가 6곳이면 아침마다 6개 중 5개가 관계없는 줄이다 → **사람은 곧 안 읽는다.**

### 제안 (사장님 결정 대기)

| | 안 | 장점 | 단점 |
|---|---|---|---|
| **가** | `Vendor` 에 `orderDays` 를 더하고 `/order` 가 **오늘 주문하는 곳만** 세운다 | 세 값이 한 군데(거래처)로 모인다. 사장님이 화면에서 다 고친다 | 프렙의 발주 항목과 거래처 중 **어느 쪽이 발주의 주인인지** 다시 정해야 한다 |
| **나** | 그대로 두고 프렙 트리거만 쓴다 | 코드 안 바뀜 | 사장님이 못 고친다. 값이 계속 두 군데 |

**저는 (가)를 권합니다.** 다만 **바꾸기 전에 실제 거래처 값이 필요합니다** —
값을 모르는 채로 필드만 만들면 맞는지 확인할 방법이 없습니다.

### 사장님께 받을 것 — 거래처마다 이 6칸

| 거래처 | 주문 방법 | **우리가 주문하는 요일** | **주문 마감 시각** | **주문 후 며칠 뒤** | **배송하는 요일** |
|---|---|---|---|---|---|
| (예) 우유 대리점 | 카톡 | 월·수·금 | 15:00 | 1일 | 화·목·토 |
| | | | | | |

★ **금요일이 다른지**를 꼭 같이 알려주세요. `day-flow.md` 에 *"금요일에 주말치까지
몰아서 주문한다"* 가 이미 적혀 있고, **금요일이 이 제품에서 가장 위험한 날**로
표시돼 있습니다.

---

## 7. 아직 확정 안 된 것 — 추측으로 채우지 않았다

| # | 무엇 | 누가 정하나 | 막히면 무슨 일 |
|---|---|---|---|
| ~~①~~ | ~~저장 실패를 조용히 넘긴다~~ | — | ✅ **해소 2026-09-09.** 아래 §7-① |
| ~~⑧~~ | ~~주기 점검 체크가 매일 지워진다~~ | — | ✅ **해소 2026-09-08.** 아래 |
| ② | **E — 거래처 6칸** (§6) | 사장님 | 리드타임 기능이 반쪽 |
| ③ | 상시 근로자 **5인 이상** 여부 | 사장님 | 연장 가산수당이 빠져 "오늘 남은 돈"이 실제보다 커 보인다 |
| ④ | 제빵·오픈조·미들의 **퇴근 시각**, 마감조 **인원** | 사장님 | S-01 의 조 카드가 실제와 다르다 |
| ⑤ | 19:00 마감 준비의 **"등등"** 나머지 항목 | 사장님 | `/prep/evening` 이 3개뿐 |
| ⑥ | 매장 태블릿 **기종·브라우저** (`color-mix()` 지원) | 사장님 | **발표 전.** 색이 안 나오면 대비가 깨진다 |
| ⑦ | 이용기록 보유기간 · PIN 분실 정책 | 사장님 | 개인정보 처리방침을 못 쓴다 |

### ① 저장 실패를 조용히 넘긴다 → ✅ **고침 (2026-09-09)**

지금 쓰는 꼴은 이렇다.

```ts
function commit(next: PunchData) {
  setPunches(next);                 // 화면은 이미 바뀌었다
  if (savePunches(next)) {          // 성공하면
    setSaved(true);                 //   "저장됨" 을 1.2초 띄운다
  }                                 // 실패하면 — 아무 말도 없다
}
```

**화면은 바뀌어 있으므로 사람은 저장된 줄 안다.** 실제로는 React 메모리에만 있고
새로고침하면 사라진다. 사파리 시크릿 모드, 저장 공간 초과, 브라우저의 사이트 데이터
차단에서 실제로 일어난다.

| 화면 | 저장 결과를 | 실패하면 |
|---|---|---|
| S-09 출퇴근 `savePunches` | 본다 | 조용함 |
| S-12 거래처 `saveVendors` | 본다 | 조용함 |
| S-14 매출 `saveSales` | 본다 | 조용함 |
| S-15 계약서 `saveContracts` | 본다 | 조용함 |
| S-11 발주 `saveOrderLog` · `saveOrderLinks` | ⛔ **안 본다** | 조용함 |
| S-10 근무표 `saveRoster` | ⛔ **안 본다** | 조용함 |
| 여러 화면 `saveSettings` | ⛔ **안 본다** | 조용함 |
| S-07 레시피 추가 `RecipeForm` | 본다 | ✅ **"저장에 실패했습니다"** ← 유일하게 맞게 한 곳 |
| S-03·04 체크·프렙 | 본다 | ⚪ **일부러 조용하다** — 체크는 잃어도 그날 일은 계속해야 한다 |

### 2026-09-10 전체 점검에서 두 가지가 더 나왔다

**ⓐ 성공 한 번이 다른 기록의 실패 경고를 지웠다.** `report(ok)` 가 대상을 몰라서
`failed` 가 boolean 하나였다 — 계약 저장이 실패한 채로 설정을 고치면 경고가 사라졌다.

**ⓑ 재시도 버튼이 화면당 하나라 엉뚱한 것을 다시 저장했다.** 계약서 화면에서
**설정** 저장이 실패했는데 버튼은 **계약**을 다시 저장했다. 거래처·발주도 같았다.

→ `report(what, ok, retry)` 로 바꾸고 `failures: SaveFailure[]` 를 돌려준다.
같은 `what` 이 성공하면 그 경고만 사라지고, **동시에 여러 개가 실패하면 전부 그린다.**

**기준(2026-09-09 정함):** `sop:punch` · `sop:contracts` · `sop:vendors` · `sop:sales` ·
`sop:roster` · `sop:settings` · `sop:orderLog` 처럼 **나중에 다시 읽어야 하는 기록**은
실패를 반드시 알린다. 체크리스트·프렙처럼 **그날 지나면 지워질 것**은 조용해도 된다.

### 무엇을 만들었나

`src/components/ui.tsx` 에 둘을 넣고 여덟 화면이 같이 쓴다.

| | |
|---|---|
| `useSaveState()` | `report(ok)` 한 줄. **성공은 1.2초 뒤 사라지고 실패는 안 사라진다** — 사람이 보고 손을 써야 하는 일이라서 |
| `SaveFailed` | 빨간 띠 + `role="alert"` + **`다시 시도`** 버튼. `Screen` 이 머리말 바로 아래에 띄운다 |

`role="alert"` 인 이유: 저장 실패는 **하던 일을 멈춰야 하는** 소식이다.
성공 알림(`저장됨`)은 `role="status"` + `aria-live="polite"` 로 하던 낭독을 안 끊는다.

| 화면 | 저장 | 실패하면 (지금) |
|---|---|---|
| S-09 출퇴근 | `savePunches` | ✅ 띠 + 다시 시도 |
| S-15 계약서 | `saveContracts` · `saveSettings` | ✅ |
| S-12 거래처 | `saveVendors` · `saveSettings` | ✅ |
| S-14 매출 | `saveSales` | ✅ |
| S-13 원가 | `saveSettings` | ✅ |
| S-11 발주 | `saveOrderLog` · `saveOrderLinks` | ✅ |
| S-10 근무표 | `saveRoster` | ✅ (`Screen` 을 안 쓰므로 `SaveFailed` 를 직접 붙였다) |
| S-08 내 레시피 **삭제** | `removeLocalRecipe` | ✅ 알림. **지우기도 저장이다** — 실패한 채로 목록으로 보내면 지운 줄 알고 나가는데 레시피는 남아 있다 |
| S-07 레시피 추가 | `addLocalRecipe` | ✅ 원래부터 맞게 돼 있었다 |
| S-04 **주기 점검** `saveCycleDone`·`saveCycleEvery` | 본다 | ✅ 띠 + 다시 시도 (2026-09-10) |
| S-03·04 **그날 체크** | — | ⚪ **일부러 조용하다.** 체크를 잃어도 그날 일은 계속해야 한다 |

**실측으로 잡은 것:** 배너를 강제로 띄워 브라우저에서 보니
`발주 기록발주 기록은 아직 남지 않았습니다` 가 나왔다.
`eun()` 이 조사만이 아니라 **낱말까지 같이** 돌려준다(`"발주 기록은"`).
`{what}{eun(what)}` 이 아니라 `{eun(what)}` 이어야 한다. 고쳤다.

**아직 안 한 것:** 단일 파일(`presentation/app.html`)은 `store.set` 이
`localStorage` 실패 시 **메모리에 담아두고 조용히 넘어간다.** 발표용 시연 파일이라
새로고침 뒤에 남아야 할 것이 없어서 그대로 뒀다.

### ⑧ 주기 점검 체크가 매일 지워졌다 → ✅ **고침 (2026-09-08)**

**주기 점검을 묶는 작업을 하다가 찾았다. 묶는 것보다 이게 훨씬 큰 문제다.**

프렙 화면은 체크를 `prep:{slug}:{영업일}` 에 저장하고, 영업일이 바뀌면
`pruneDayKeys()` 가 지난 키를 지운다. **주기 점검도 같은 규칙을 탄다.**

```
2026-09-08  정수 필터 교체 ✅ 체크        → prep:cycle:2026-09-08
2026-09-09  화면을 열면 — 체크가 없다      → prep:cycle:2026-09-09 (빈 키)
```

**즉 `4개월마다` 라고 적혀 있지만 앱은 그 4개월을 세지 않는다.**
마지막으로 언제 했는지를 아무데도 남기지 않으므로, 이 화면은 지금
**읽을 거리 13줄**이지 기능이 아니다.

`day-flow.md` 가 이 항목들을 이렇게 설명한다 — *"며칠 걸러도 티가 안 납니다.
그래서 아무도 기억 못 합니다."*
**기억을 대신하는 것이 이 화면의 존재 이유인데, 그걸 못 하고 있다.**

**고치려면 무엇이 필요한가**

| | |
|---|---|
| 저장 | 날짜별 체크가 아니라 **마지막 실행일** — 예: `sop:cycleDone` = `{ "c-1": "2026-05-11" }` |
| 지우기 | `pruneDayKeys` 대상에서 **빼야 한다** (`sop:punch` 처럼 보존) |
| 화면 | `4개월마다` 옆에 **`마지막 5/11 · 27일 남음`**, 지났으면 **`13일 지났습니다`** |
| 정렬 | 남은 날짜가 적은 것부터 — 오늘 할 게 위로 온다 |
| 내보내기 | `/backup` 에 넣을지 결정 필요 (보건증·소방은 점검 기록이 증빙이 된다) |

### 무엇을 만들었나 — `src/lib/cycleDone.ts`

| | |
|---|---|
| 저장 | `sop:cycleDone` = `{ "c-1": "2026-05-11" }`. **날짜가 키가 아니라 값이라서** `pruneDayKeys` 에 안 걸린다 |
| 체크의 뜻 | "오늘 했다". 오늘 것을 다시 누르면 취소되고, **지난 기록을 누르면 오늘로 갱신**된다 (취소가 아니다) |
| 화면 | `마지막 5/11 · 1일 지났습니다`(빨강) / `마지막 9/8 · 6일 남음` / `마지막 5/11 · 121일 전`(주기 미정) / `기록 없음 — 언제 했는지 모릅니다`(빨강) |
| 입력 | 항목마다 **`마지막 [날짜]` + `주기 [n]일`**. 날짜 칸이 있어야 **지난 기록을 채워 넣을 수 있다** |
| 상단 | `되돌릴 수 없는 것` 안내를 **`지금 해야 할 것 n개`** 로 바꿨다 — 13개 중 1개만 되돌릴 수 없어서 그 안내는 여기서 뜻이 약했다 |
| 진행률 | `n/13` 의 `n` 은 **기한 안에 있는 것**이다. "오늘 체크한 개수" 는 4개월 주기에서 늘 0 이라 뜻이 없다 |
| 정렬 | 급한 것부터. **기록 없는 것이 맨 앞** |

**기록이 없으면 `null` 로 둔다.** 0 으로 채우면 "오늘 해야 함", 큰 수로 채우면
"여유 있음" 으로 보인다. **둘 다 거짓이다** — 모른다는 것을 화면이 그대로 말한다.
소화기 점검을 3년 전에 했는지 어제 했는지 모르는 상태가 가장 위험하고,
그걸 목록 뒤로 밀면 사장님이 영영 안 채운다.

### ★★ 주기 숫자는 시드에서 뺐다 — 2026-09-08

사장님 지적: *"제빙기 청소 1개월마다 체크도 지우지 — 더럽게. 매일 청소하는 곳도
있는데. 6개월마다 이런 거도 쓰는 곳마다 다 달라서 굳이 없어도 될 거 같은데."*

**맞는 지적이다.** 제빙기를 매일 닦는 매장에 `1개월마다` 를 띄우면 그 화면은
**처음부터 틀린 말**을 한다. `4개월마다`·`6개월마다` 도 기계·계약·업종에 따라 다르다.
**내가 모르는 숫자를 박아놓고 앱이 단정하면, 사장님이 그 숫자를 믿는다.**

| 무엇이 바뀌었나 | |
|---|---|
| 시드의 `everyDays` | 13개 전부 **`null`** (`tests/seed.test.ts` 가 못 박는다) |
| 주기 뱃지(`1개월마다`) | 주기 항목에서는 **안 그린다** |
| 주기를 안 정했을 때 | 기한을 판단하지 **않는다.** `마지막 5/11 · 121일 전` 만 |
| 주기를 넣으면 | 그때부터 `27일 남음` / `13일 지났습니다` 로 바뀐다 |
| 정렬 | 기록 없음 → 기한 급한 것 → **주기 미정(판단 불가)은 맨 아래** |

**주기가 없어도 값이 남는다.** `day-flow.md` 가 말하는 문제는
*"며칠 걸러도 티가 안 나서 아무도 기억 못 한다"* 인데,
**마지막으로 한 날만 있으면 그 문제는 이미 풀린다.** 기한 판정은 그 위의 덤이다.

### 손본 항목 셋

| 항목 | 무엇을 |
|---|---|
| **정수 필터** | 제목을 `정수 필터 (렌탈이면 담당자 연락)` 로. 렌탈이면 교체는 업체가 하고 **연락만 하면 된다**. 이 항목의 목적은 *언제 갈았는지 적어두는 것* 이다 |
| **제빙기 청소** | 설명에 *"매일 닦는 매장도 있습니다 — 주기는 매장에서 정해 주세요"* |
| **법정 3개** | 설명에 **주기가 업종·규모에 따라 다르다**는 사실과 **어디서 확인하는지**를 적었다. 숫자는 안 적었다 — 아래 §7-⑨ |

### ⑨ 법정 주기를 앱이 단정하지 않는다 ⚠️ **사장님 확인 필요**

사장님 질문: *"보건증, 소방시설, 위생교육도 1년 확실함?"*

**확실하지 않습니다. 그래서 앱에서 숫자를 뺐습니다.**

| | 제가 아는 것 | 왜 못 박을 수 없나 |
|---|---|---|
| **보건증** (건강진단결과서) | 식품위생법상 식품접객업 종사자는 **1년** 주기가 표준으로 알려져 있다 | 업종·직무에 따라 대상과 항목이 다르다. **결과서에 유효기간이 찍혀 나오므로 그걸 보는 게 정확하다** |
| **위생교육** | **영업자**는 매년 교육이 표준. 신규 영업자는 별도(사전 교육) | **영업자와 종업원의 대상·시간이 다르다.** 업종별 협회에 따라 운영도 다르다 |
| **소방시설** | 소화기 **육안 점검은 월 1회** 권장, 내용연한이 따로 있다. 자체점검(작동/종합)은 **연 단위** | ★ **자체점검 대상 여부와 주기가 건물 규모·용도에 따라 갈린다.** 소규모 근린생활시설은 대상이 아닐 수도 있다. **관할 소방서에 물어야 한다** |

**저는 법을 확인해 드릴 수 없습니다** — 조문 번호까지 외워서 말하면 그게 맞는지
제가 검증할 방법이 없고, 틀리면 과태료가 나갑니다.
**관할 보건소·소방서·업종 협회에서 확인한 값을 화면의 `주기` 칸에 넣어 주세요.**
그러면 그때부터 앱이 셉니다.

테스트 21개 (`tests/cycleDone.test.ts`). 그중 하나는
**영업일이 바뀌어도 안 지워지는지**를 `pruneDayKeys` 를 직접 돌려 확인한다.

⚠️ 오후 프렙·마감 준비는 **하루 단위가 맞다.** 이 문제는 주기 점검만의 것이었다.

⚠️ **아직 안 정한 것:** 이 기록을 `/backup` 에 넣을지. 보건증·소방·위생교육은
점검 기록 자체가 증빙이 된다.

---

## 8. Figma — 어디까지 됐나 ⚠️ **1/5 에서 막혔다**

| | |
|---|---|
| 파일 | **매장수첩 · 핵심 화면 5 (와이어프레임)** |
| 주소 | https://www.figma.com/design/3beANZEtkFcdfgaan2ZJZR |
| 만든 것 | 제목 블록 + **S-01 오늘** 아트보드 (375×656, 오토레이아웃, Noto Sans KR) |
| 못 만든 것 | **S-04 프렙 · S-03 체크리스트 · S-11 발주 · S-09 출퇴근 (4개)** |

**막힌 이유:** Figma **Starter 요금제의 MCP 호출 한도**에 걸렸다.
`You've reached the Figma MCP tool call limit on the Starter plan.`
화면 하나를 만든 직후 한도가 닫혀서 **스크린샷 검증도 못 했다** — 즉
S-01 아트보드가 눈으로 보기에 제대로 나왔는지 **확인 안 됐다.**

**대안은 이미 있다.** [../wireframes/와이어프레임.html](../wireframes/와이어프레임.html) 에
**5개 화면이 전부** 들어 있고 주석까지 붙어 있다. Figma 는 같은 내용을 옮기는 작업이다.

**풀 방법 셋:**

| | 방법 | 비고 |
|---|---|---|
| 가 | 한도가 풀릴 때까지 기다렸다가 이어서 만든다 | 언제 풀리는지는 Figma 쪽 정책이라 내가 모른다 |
| 나 | 요금제를 올린다 | 발표(9/18) 하나 때문에 올릴 값어치가 있는지는 사장님 판단 |
| 다 | **HTML 와이어프레임으로 간다** | 이미 완성돼 있고 인쇄도 된다. **저는 이걸 권합니다** |

★ **Figma 가 이 프로젝트에 꼭 필요한 물건은 아니다.** 화면을 만드는 사람과
보는 사람이 같은 한 사람이고, 디자이너에게 넘길 계획이 아직 없다.
**"화면·상태·이동의 합의문"이라는 목적은 HTML 쪽이 이미 채웠다.**

---

## 관련 문서

- 흐름: [20_기능흐름도.md](20_기능흐름도.md) · [14_사용자흐름도.md](14_사용자흐름도.md)
- 클릭 흐름: [22_클릭흐름.md](22_클릭흐름.md)
- 와이어프레임: [../wireframes/와이어프레임.html](../wireframes/와이어프레임.html)
- 기존 감사 기록: [02_화면설계서.md](02_화면설계서.md)
- 정책: [19_정책정의서.md](19_정책정의서.md)
- 하루 흐름: [../day-flow.md](../day-flow.md)
