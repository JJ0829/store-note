# 17. 요구사항 정의서 — 매장수첩

| | |
|---|---|
| 작성일 | **2026-09-08** |
| 기준 커밋 | `0be2bf4` |
| 자매 문서 | 기능 동작은 `13_기능명세서.md`, 검증 조건은 `15_인수기준.md`, 계층은 `16_정보구조도.md` |

> ### 이 문서가 채우는 두 칸
>
> **① 기능 요구사항이 `01_MVP기획서.md` §6 안에 묻혀 있었다.** 기획서는 "무엇을 만들었나"를
> 설명하는 문서라, 요구사항으로 인용하려면 매번 §6 을 찾아 들어가야 했다. 여기로 꺼낸다.
>
> **② ★ 비기능 요구사항이 문서 전체에 0건이었다.** `docs/` 를 전수 검색해도
> "비기능"·"응답 시간"·"동시 사용"·"지원 범위" 라는 절이 없다. 성능·브라우저 최저선·
> 저장 한계가 **아무데도 안 적혀 있었다.** §3 이 그것이고, 이 문서의 실질적 기여다.
>
> ⚠️ **없는 요구사항을 만들어 넣지 않았다.** 근거가 없으면 ⛔ 로 비워뒀다.

---

## 1. 요구사항 ID 규칙

| 접두사 | 뜻 | 개수 |
|---|---|---|
| **FR** | 기능 요구사항 | 22 |
| **NFR** | 비기능 요구사항 | 14 |
| **CO** | 제약 조건 — 바꿀 수 없는 전제 | 8 |

각 항목의 **검증** 칸은 `15_인수기준.md` 의 AC 번호 또는 테스트 파일을 가리킨다.
검증 칸이 비면 **"확인 방법이 없다"**는 뜻이다.

---

## 2. 기능 요구사항 (FR)

### 2.1 발표 전 필수 — D-008 이 정한 MUST

| ID | 요구사항 | 상태 | 검증 |
|---|---|---|---|
| **FR-01** | 데이터 모델 + 시드가 카페·베이커리여야 한다 (그릴·튀김이 뜨면 서사가 깨진다) | ✅ | `seed.test.ts` 14개 |
| **FR-02** | **프렙 화면이 리드타임을 시각으로 계산한다** — "지금 걸면 → 내일 07:43부터" | ✅ | AC-03 · `leadTime.test.ts` 25개 |
| **FR-03** | 레시피 화면이 배수를 환산한다 (0.5~3배, 재료 g 까지) | ✅ | `scale.test.ts` 8개 |
| **FR-04** | 시연용 제한 배포 (`noindex` + `robots.txt`) | 🟠 **부분** — `noindex` 전역·`robots.txt` 있음, **배포 자체는 미착수** | `06` V-09 |

### 2.2 제품의 원래 목적 — 신입 온보딩

| ID | 요구사항 | 상태 | 검증 |
|---|---|---|---|
| **FR-05** | 신입이 **링크만으로** 체크리스트를 열 수 있다 (로그인 없이) | ✅ | AC-07 |
| **FR-06** | 교육 모드가 한 화면에 한 항목씩 보여준다 (태블릿) | ✅ | ⛔ **테스트 없음** |
| **FR-07** | **필수(`critical`) 항목은 확인 없이 넘어갈 수 없다** | ✅ | AC-01 · ⛔ 자동 테스트 없음 |
| **FR-08** | 공용 태블릿에서 앞사람 진도가 다음 신입에게 안 보인다 | ✅ | AC-01 #6 |
| **FR-09** | 체크 상태가 날짜별로 초기화된다 | ✅ | AC-03 #6 |

### 2.3 계획 밖에서 들어온 것 — 운영 기능 7종 (2026-09-04)

> ⚠️ **이 7개는 D-008 의 MUST/SHOULD/LATER 어디에도 없었다.** 작업 중 요청으로 들어왔다.
> `01` §6.1-A 가 등급을 사후에 붙이지 않기로 한 판단을 여기서도 지킨다 —
> **등급 대신 "요구사항으로 확정됨"으로만 적는다.**

| ID | 요구사항 | 상태 | 검증 |
|---|---|---|---|
| **FR-10** | 출퇴근을 버튼 두 개로 찍고, 근무표와 비교해 지각·결근·연장을 판정한다 | ✅ | AC-05 · `attendance.test.ts` 23개 |
| **FR-11** | 근로계약 조건을 넣으면 **법정 점검**(서면교부·최저임금·주휴)을 해준다 | ✅ | `contracts.test.ts` 14개 |
| **FR-12** | 매출은 **세 칸만** 입력받고 인건비는 묻지 않는다 (출퇴근×시급에서 계산) | ✅ | AC-04 · `sales.test.ts` 15개 |
| **FR-13** | 레시피 원가를 거래처 단가에서 계산하고, **못 구한 값을 0원으로 세지 않는다** | ✅ | AC-04 · `cost.test.ts` 14개 |
| **FR-14** | 발주를 **주문함 / 들어옴 두 단계**로 남기고, 안 들어온 것을 아침에 띄운다 | ✅ | `orders.test.ts` 19개 |
| **FR-15** | 거래처 마감 시각·배송 요일로 **도착일**을 계산한다 (주말 배송 제외) | ✅ | `vendors.test.ts` 12개 |
| **FR-16** | 매출·시급·단가·계약을 **사장님만** 볼 수 있다 | 🟠 **가리개 수준** | AC-02 · `pinGate.test.ts` 12개 |

### 2.4 그 뒤에 확정된 것

| ID | 요구사항 | 상태 | 검증 |
|---|---|---|---|
| **FR-17** | **레시피는 링크만으로 열리지 않는다** (매장 번호) — 영업비밀 | 🟠 **가림막.** 소스에는 남는다 | AC-02 #8 |
| **FR-18** | 근무표는 개인정보 5종을 담으므로 사장님만 본다 | ✅ | `06` V-20 |
| **FR-19** | **출퇴근·근로계약을 파일로 빼낼 수 있다** — 근로기준법 제42조 3년 보존 | ✅ | AC-06 · `backup*.test.ts` 30개 |
| **FR-20** | 되돌리기는 **합치지 않고 덮어쓰며**, 잃는 양을 먼저 보여준다 | ✅ | AC-06 #8·#9 |
| **FR-21** | 없는 주소로 들어오면 **무엇을 하면 되는지** 알려준다 | ✅ | AC-07 #1·#2 |
| **FR-22** | 촬영해야 할 항목 목록과 진행률을 보여준다 | ✅ | `mediaProbe.test.ts` 15개 |

### 2.5 요구사항이 아닌 것 — 명시적으로 제외

| 제외 항목 | 이유 | 근거 |
|---|---|---|
| 회원가입·로그인 | 신입이 첫날 가입할 확률보다 링크 누를 확률이 높다 | `CLAUDE.md` 확정 |
| 네이티브 앱 | 같은 이유. 모바일 웹 + 필요하면 PWA | `CLAUDE.md` |
| 결제·환불·구독 | ⛔ **B4 미해결.** 점주에게 금액을 물어본 적이 0회 | `01` §9 |
| 알림(푸시·문자) | 서버가 없다 | D-007 뒤 |
| 제재·신고 | 사용자가 0명 | — |
| **주민등록번호 저장** | 개인정보보호법 제24조의2가 암호화를 요구하는데 못 맞춘다. **요청이 와도 만들지 않는다** | `contracts.ts:1-24` |
| 프랜차이즈 본사 기능 | 본사가 사진 매뉴얼을 이미 준다 — 해결된 문제 | `CLAUDE.md` |

---

## 3. ★ 비기능 요구사항 (NFR) — 이 문서가 처음 적는 것

> `docs/` 전수 검색 결과 **"비기능"·"응답 시간"·"동시 사용"·"지원 범위" 절이 0건**이었다.
> 아래는 **코드·실측에서 역으로 뽑은 것**이고, 목표치가 아니라 **지금 사실**이다.
> 목표치를 정해야 하는 칸은 ⛔ 로 남겼다.

### 3.1 오프라인 · 가용성

| ID | 요구사항 | 실측 | 검증 |
|---|---|---|---|
| **NFR-01** | 시연용 단일 파일은 **외부 네트워크 요청이 0건**이어야 한다 | ✅ `grep -c 'https\?://' presentation/*.html` → **0** | AC-4 #3 |
| **NFR-02** | 단일 파일은 서버·설치·인터넷 없이 더블클릭으로 동작한다 | ✅ 확인됨 | 육안 |
| **NFR-03** | 저장소 접근이 막혀도(사생활 보호 모드·용량 초과) **화면이 죽지 않는다** | ✅ `store.ts` 전 함수 try/catch | AC-07 #3 · `settings`·`localRecipes` 테스트 |
| **NFR-04** | 클립보드가 막히면 3단 폴백 후에도 예외를 던지지 않는다 | ✅ | `copyText.test.ts` 8개 |
| **NFR-05** | `/api/media` 가 실패해도 촬영 보드가 죽지 않는다 | ✅ | `mediaProbe.test.ts` 2개 |
| **NFR-06** | 정전·기기 고장 시 대체 수단 | ❌ **없다.** 종이가 우리보다 나은 지점 — 숨기지 않는다 | `01` §4.6 |

### 3.2 저장 · 데이터 규모

| ID | 요구사항 | 실측 · 근거 |
|---|---|---|
| **NFR-07** | 3년 보존 대상이 브라우저 저장 한계 안에 들어와야 한다 | ✅ **여유 있다.** 출퇴근 1건 = **117바이트**(JSON 실측) → 직원 5명 × 3년(1095일) = **0.61MB**, 10명이면 1.22MB. `localStorage` 통상 한도 5MB |
| **NFR-08** | 시드 데이터 규모 | 포지션 3 · 스텝 26 · 레시피 10 · 프렙 항목 30 · 근무조 4 · `seed.json` **61KB** (숫자 정본: [21_화면명세.md §1-b](21_화면명세.md)) |
| **NFR-09** | ⚠️ 저장은 **기기 하나**에 갇힌다. 교대 인계·다기기 동기화가 안 된다 | 알려진 한계. D-007 뒤 Supabase |
| **NFR-10** | ⛔ **저장 용량이 한도에 가까워질 때의 동작이 정의돼 있지 않다** | `saveJson` 이 `false` 를 돌려주는 것까지만. **사용자에게 무엇을 하라고 할지 미정** |

### 3.3 브라우저 최저선 — 코드가 실제로 요구하는 것

| ID | 요구사항 | 근거 (실측) |
|---|---|---|
| **NFR-11** | **단일 파일**은 `color-mix()` 를 6곳에서 쓴다 → **Chrome 111+ / Safari 16.2+** | `grep -cF 'color-mix(' presentation/매장수첩.html` = 6 |
| **NFR-12** | 단일 파일은 `env(safe-area-inset)` 2곳, `backdrop-filter` 2곳 | 같은 방식 실측 |
| **NFR-13** | **Next 앱**은 `dvh` 를 15곳에서 쓴다 → **Chrome 108+ / Safari 15.4+** | `grep -rF dvh src/` = 15 |
| **NFR-14** | 옵셔널 체이닝 35곳 · 널 병합 63곳 → **ES2020 이상.** `tsconfig` target 은 `ES2022` | `tsconfig.json:3` |

> ⚠️ **매장 태블릿이 이 최저선을 넘는지 확인한 적이 없다.**
> 오래된 갤럭시탭이나 iPad 2 세대라면 `color-mix()` 가 안 돌아 **단일 파일의 주 버튼 색이 빠진다.**
> → ❓ **사장님 확인 필요: 시연·실증 매장 태블릿의 기종과 브라우저 버전**

### 3.4 접근성

| ID | 요구사항 | 상태 |
|---|---|---|
| **NFR-15** | 본문 대비 WCAG AA 4.5:1 이상 | ✅ 단일 파일은 2026-09-07 에 맞췄다 (`--ink-3` 5.02~5.70, 주 버튼 4.95) |
| **NFR-16** | 필수 조작을 키보드만으로 완주할 수 있다 | ✅ 단일 파일 확인. ⚠️ **Next 앱은 미확인** |
| **NFR-17** | 상태 변화(오류·저장·진행)를 스크린리더가 읽는다 | ✅ **해소 2026-09-09.** 양쪽 다. Next 앱에 `role="alert"` 8곳(잠금 오류·복원 실패) + `role="status"` 2곳(저장됨·복원 완료) |
| **NFR-18** | 2차원 표의 행·열 머리글이 연결돼 읽힌다 | ✅ **해소 2026-09-09.** Next 앱 `scope` 18곳. 근무표·출퇴근의 행 머리글을 `<td>`→`<th scope="row">` 로 승격(`font-weight:400` 으로 화면은 불변). 브라우저 실측: 셀이 "김제빵 / 9/7(월)" 로 연결돼 읽힌다 |

> ~~접근성이 두 앱에서 갈렸다~~ → ✅ **2026-09-09 에 맞췄다.** 9/7 작업이 단일 파일만 대상이었던 것을 제품 본체에도 적용했다.
> 남은 것은 **NFR-16(키보드 완주)** — Next 앱은 아직 손으로 확인하지 않았다.

### 3.5 성능 — ⛔ 목표치가 없다

| ID | 요구사항 | 상태 |
|---|---|---|
| — | 화면 첫 표시 시간 | ⛔ **목표 미정.** 측정한 적 없음 |
| — | 동시 사용자 수 | **해당 없음** — 서버가 없어 기기별로 독립 |
| — | 데이터 증가에 따른 화면 지연 | ⛔ 미측정. 프렙 30항목·레시피 10개 규모에서는 문제가 드러날 수 없다 |

> **성능 목표를 지금 만들어 적지 않는다.** 사용자가 0명이고 데이터가 시드뿐이라
> 어떤 숫자를 써도 근거가 없다. **실증 매장 투입 후 측정해서 채울 칸이다.**

---

## 4. 제약 조건 (CO) — 바꿀 수 없는 전제

| ID | 제약 | 근거 | 영향 |
|---|---|---|---|
| **CO-01** | **서버가 없다.** 전부 브라우저 저장 | D-007 미확정 | API 명세 불가 · 진짜 접근통제 불가 · 지표 수집 0건 · 교대 인계 불가 |
| **CO-02** | 런타임 의존성 **3개**만 (`next` `react` `react-dom`) | `package.json` 실측 | 외부 UI·차트·AI SDK 없음. 직접 만들어야 한다 |
| **CO-03** | **태블릿이 1순위 기기** | `CLAUDE.md` 확정 (사용자 확인) | 개인폰은 젖은 손·위생 규정으로 불리 |
| **CO-04** | 경쟁자는 **벽에 붙은 코팅 종이** — 무료·즉시·오프라인 | `CLAUDE.md` | *체크리스트를 준다*는 것 자체는 새 가치가 0 |
| **CO-05** | 발표 **2026-09-18**. 이 날짜가 우선순위를 정한다 | D-008 | 회귀 위험 있는 작업은 발표 후 |
| **CO-06** | 저장소는 **비공개**. 레시피가 영업비밀 | `CLAUDE.md` | 공개로 바꾸지 않는다 |
| **CO-07** | **주민등록번호를 저장하지 않는다** | 개인정보보호법 제24조의2 | 계약서 원본은 앱 밖 |
| **CO-08** | ⛔ **B4 미해결** — 점주가 왜 돈을 내는지 근거 0 | `01` §9 | 가격·과금·정책 정의서를 못 쓴다 |

---

## 5. 이 문서가 드러낸 것

| # | 무엇 | 크기 | 언제 |
|---|---|---|---|
| **1** | **⛔ 매장 태블릿 기종·브라우저 버전을 모른다** | ❓ 확인만 하면 됨 | **발표 전** — `color-mix()` 가 안 되면 시연 화면 색이 빠진다 |
| ~~2~~ | ~~접근성이 두 앱에서 갈렸다~~ | ✅ **해소 2026-09-09** — `scope` 18곳 · `role=alert` 8 · `role=status` 2 | — |
| **3** | FR-06·FR-07(교육 모드·필수 게이트)에 **자동 테스트가 없다** | 큼 — React 테스트 환경 | 발표 후 |
| **4** | ⛔ 저장 용량 한도 도달 시 사용자에게 무엇을 할지 미정 (NFR-10) | 작음 — 문구 | 배포 전 |
| **5** | ⛔ 성능 목표치가 없다 | — | **실증 후.** 지금 쓰면 근거 없는 숫자 |

**발표에 직접 걸리는 것은 1번뿐이다.** 나머지는 발표 후이거나 실증 후다.

---

## 갱신 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-08 | 최초 작성. FR 22 · NFR 14 · CO 8. **비기능 요구사항이 문서에 0건이던 것을 실측으로 채웠다** — 출퇴근 1건 117바이트/3년 0.61MB, 브라우저 최저선(`color-mix` Chrome 111+ · `dvh` Chrome 108+). 성능 목표치는 근거가 없어 비워뒀다 |
